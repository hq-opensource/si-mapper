\section{A Solution: Agentic AI for Building Semantic Modeling}
\label{sec:solution}

The previous section established that manual BACnet point mapping is costly, labor-intensive, and
resistant to automation with existing tools.
This section explains why agentic AI has a good chance of solving the problem, situates SI-Mapper within
the landscape of prior work, and introduces the high-level pipeline of operation.

\subsection{Existing Approaches and Their Limitations}
\label{subsec:existing-approaches}

Several approaches have been applied to the point mapping and building digitization problem.
Each addresses part of the challenge but leaves a significant gap.
Table~\ref{tab:existing-approaches} summarizes the state of the art.

\begin{table}[htbp]
\centering
\caption{Existing approaches to building point mapping and semantic model generation.}
\label{tab:existing-approaches}
\small
\begin{tabular}{p{3.5cm}p{4.5cm}p{5cm}}
\toprule
\textbf{Approach} & \textbf{What It Does} & \textbf{Why It Falls Short} \\
\midrule
Manual expert mapping & Engineer maps all points by hand using drawings and BMS documentation & \$100--\$500 per point (software costs alone)\cite{mathew2015eis}; weeks per building; requires scarce HVAC expertise; error-prone \\
\addlinespace
Vendor proprietary tools & BMS vendors (e.g., Siemens, Johnson Controls) provide semi-automated configuration within their ecosystem & Vendor lock-in; does not produce semantic models; tied to new installations only; no interoperability \\
\addlinespace
Tag-based normalization & Rule-based scripts parse point names using regex or keyword matching & Brittle; breaks with naming convention variations; captures equipment types only, no topology \\
\addlinespace
Building Information Modeling (BIM) & Architectural models capture equipment geometry and spatial layout & Created during design; diverges from as-built reality; no BACnet operational data; requires BIM expertise \\
\addlinespace
BrickLLM (2025) & Large language model converts natural language building descriptions to Brick RDF\cite{perini2025brickllm} & Requires accurate natural language input; no visual drawing analysis; no BACnet integration; produces Brick, not ASHRAE 223P \\
\addlinespace
LLM point classification & LLMs classify individual BACnet point names into ontology tags & Classifies individual points only; does not reconstruct topology; not end-to-end \\
\bottomrule
\end{tabular}
\end{table}

The gap is clear: no prior system combines visual HVAC drawing analysis, automated BACnet CSV
point-to-equipment mapping, full ASHRAE 223P topology generation, and iterative self-correction in
a single pipeline.
The closest academic work, BrickLLM~\cite{perini2025brickllm}, addresses only
natural language input to Brick RDF conversion and does not address the drawing recognition problem,
BACnet CSV parsing, or ASHRAE 223P topology generation.

\subsection{SI-Mapper: An Agentic Approach}
\label{subsec:si-mapper-overview}

SI-Mapper takes two raw building data inputs, HVAC engineering drawings (PNG or PDF) and BACnet
point exports (CSV files), and produces two outputs: an ASHRAE 223P semantic model in Turtle (TTL)
format, and an interactive graph visualization of that model in a web interface.

The entire pipeline is orchestrated by an AI agent application guided by domain-specific skill instructions.
Figure~\ref{fig:pipeline} illustrates the data flow.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{pipeline.png}
\caption{SI-Mapper data pipeline --- from raw building data to semantic model and graph visualization.}
\label{fig:pipeline}
\end{figure}

The key enabler is multimodal AI.
Recent large language models like Gemini 3.1 and Claude Sonnet 4.6 can analyze engineering
drawings directly, identifying equipment symbols, ductwork connections, and spatial relationships from
visual inputs.
HVAC schematic diagrams, with their regular symbol vocabulary and consistent layout conventions, are
more tractable for AI recognition than general architectural floor plans.
The insight from SI-Mapper's development is that standard models underperform on domain-specific
drawing recognition until guided by detailed, domain-specific instructions the \emph{skills}
architecture described in Section~\ref{sec:implementation}.

SI-Mapper's novelty lies in combining four capabilities that no prior system integrates:
\begin{enumerate}[leftmargin=*, itemsep=2pt]
  \item \textbf{Multimodal drawing recognition:} AI analysis of HVAC engineering drawings to extract
        equipment and ductwork topology.
  \item \textbf{BACnet CSV mapping:} automated assignment of BACnet point data to the equipment
        identified in the drawings.
  \item \textbf{ASHRAE 223P generation:} production of a formally valid ASHRAE 223P ontology
        including ConnectionPoints, Connections, mediums, and telemetry links.
  \item \textbf{Iterative self-correction:} the agent compares its generated model against validation
        rules, identifies errors, and corrects them autonomously --- looping until the model is valid.
\end{enumerate}

This approach replaces weeks of expert engineering effort with an automated pipeline that runs in
minutes, producing a machine-readable semantic model that unlocks software portability, advanced
analytics, and grid integration for the building.
Section~\ref{sec:implementation} describes the engineering decisions and experimental iterations that
led to this architecture.
---
phase: 08-implement-capture-frontend-state-visual-verification-tool
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - agent/master_architecture/prompts/master_instruction.md
  - agent/tests/test_capture_frontend_state.py
autonomous: true
requirements: [VIS-02]

must_haves:
  truths:
    - "master_instruction.md contains a Visual Verification Protocol section with the 2-step capture+load sequence"
    - "Unit test proves the tool returns correct dict structure on success and error paths"
  artifacts:
    - path: "agent/master_architecture/prompts/master_instruction.md"
      provides: "Agent instructions for visual verification"
      contains: "Visual Verification Protocol"
    - path: "agent/tests/test_capture_frontend_state.py"
      provides: "Unit tests for tool logic (mocked Playwright)"
      min_lines: 40
  key_links:
    - from: "agent/master_architecture/prompts/master_instruction.md"
      to: "capture_frontend_state"
      via: "tool name reference in instructions"
      pattern: "capture_frontend_state"
    - from: "agent/master_architecture/prompts/master_instruction.md"
      to: "load_artifacts"
      via: "artifact_names reference"
      pattern: "load_artifacts.*artifact_names.*verification/latest_snapshot\\.png"
---

<objective>
Add the Visual Verification Protocol section to master_instruction.md so the agent knows when and how to use the capture tool, and create unit tests that validate tool behavior with mocked Playwright.

Purpose: Without the instruction, the agent will never call the tool. Without tests, regressions go undetected.
Output: Updated master_instruction.md, new test file.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-implement-capture-frontend-state-visual-verification-tool/08-CONTEXT.md
@.planning/phases/08-implement-capture-frontend-state-visual-verification-tool/08-01-SUMMARY.md

<interfaces>
From agent/master_architecture/tools/capture_frontend_state_tool.py (created in 08-01):
```python
class CaptureFrontendStateTool(BaseTool):
    # name='capture_frontend_state'
    # No parameters
    async def run_async(self, *, args: dict[str, Any], tool_context: ToolContext) -> dict[str, Any]:
        # Returns {"status": "success", "artifact": "verification/latest_snapshot.png", "message": "..."}
        # Or {"status": "error", "message": "Could not capture canvas snapshot: <reason>"}
        ...

capture_frontend_state_tool = CaptureFrontendStateTool()
```

From agent/master_architecture/prompts/master_instruction.md (current content, 48 lines):
- Ends with a "STOP" instruction about loading skills before acting
- New section goes BEFORE the final "STOP" paragraph
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Visual Verification Protocol to master_instruction.md</name>
  <files>agent/master_architecture/prompts/master_instruction.md</files>
  <read_first>
    agent/master_architecture/prompts/master_instruction.md
    .planning/phases/08-implement-capture-frontend-state-visual-verification-tool/08-CONTEXT.md
  </read_first>
  <action>
Insert a new section in `agent/master_architecture/prompts/master_instruction.md` BEFORE the final "**STOP.**" paragraph (before line 47). Add the following markdown block:

```markdown
## Visual Verification Protocol

When you believe you have finished placing all components for a task, you MUST verify your work visually before declaring the task complete:

1. Call `capture_frontend_state()` to take a screenshot of the live Graphivac canvas.
2. Call `load_artifacts(artifact_names=["verification/latest_snapshot.png"])` to inspect the screenshot.
3. Compare the canvas snapshot against the original reference image. Check that:
   - All expected components are visible on the grid
   - Components are positioned at the correct coordinates
   - Flow directions and connections appear correct
   - No phantom or misplaced components exist
4. If discrepancies are found, correct the placement using the internal grid tools, then repeat from step 1.
5. Only declare the task complete after visual confirmation matches the reference.

If `capture_frontend_state()` returns an error, proceed without visual verification and note the failure in your response.
```

The final "**STOP.**" paragraph must remain as the very last block in the file.
  </action>
  <verify>
    <automated>grep -n "Visual Verification Protocol" /home/juan/codes/si-mapper/agent/master_architecture/prompts/master_instruction.md && grep -n "capture_frontend_state" /home/juan/codes/si-mapper/agent/master_architecture/prompts/master_instruction.md && grep -n 'load_artifacts.*artifact_names.*verification/latest_snapshot' /home/juan/codes/si-mapper/agent/master_architecture/prompts/master_instruction.md</automated>
  </verify>
  <acceptance_criteria>
    - master_instruction.md contains `## Visual Verification Protocol`
    - master_instruction.md contains `capture_frontend_state()` (the tool call instruction)
    - master_instruction.md contains `load_artifacts(artifact_names=["verification/latest_snapshot.png"])`
    - master_instruction.md contains `you MUST verify your work visually`
    - The `**STOP.**` paragraph is still the last block in the file
    - The new section appears BEFORE `**STOP.**`
  </acceptance_criteria>
  <done>master_instruction.md has Visual Verification Protocol section with exact 2-step sequence (capture then load_artifacts), positioned before the STOP block</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for capture_frontend_state tool</name>
  <files>agent/tests/test_capture_frontend_state.py</files>
  <read_first>
    agent/master_architecture/tools/capture_frontend_state_tool.py
    agent/master_architecture/tools/ingest_category_tool.py
  </read_first>
  <behavior>
    - Test 1 (success path): When Playwright works, run_async returns {"status": "success", "artifact": "verification/latest_snapshot.png", "message": contains "Snapshot saved"}
    - Test 2 (success path): tool_context.save_artifact is called with filename="verification/latest_snapshot.png" and a types.Part with mime_type="image/png"
    - Test 3 (error path): When Playwright raises an exception, run_async returns {"status": "error", "message": contains "Could not capture canvas snapshot"}
    - Test 4 (declaration): _get_declaration returns FunctionDeclaration with name="capture_frontend_state"
  </behavior>
  <action>
Create `agent/tests/test_capture_frontend_state.py` with pytest + unittest.mock:

```python
import pytest
import asyncio
from unittest.mock import AsyncMock, MagicMock, patch
import os

# Set env vars BEFORE importing the tool
os.environ.setdefault("GRAPHIVAC_BASE_URL", "https://graphivac.hvac.io/api/v1")
os.environ.setdefault("GRAPHIVAC_ORG_ID", "test-org")
os.environ.setdefault("GRAPHIVAC_PROJECT_ID", "test-project")
os.environ.setdefault("GRAPHIVAC_GRID_ID", "test-grid")

from master_architecture.tools.capture_frontend_state_tool import (
    CaptureFrontendStateTool,
    capture_frontend_state_tool,
)
```

**test_get_declaration:**
- `tool = CaptureFrontendStateTool()`
- `decl = tool._get_declaration()`
- Assert `decl.name == "capture_frontend_state"`

**test_run_async_success:**
- Create a mock `tool_context` with `save_artifact = AsyncMock(return_value=1)`
- Mock `playwright.async_api.async_playwright` context manager:
  - `page.screenshot = AsyncMock(return_value=b"fake_png_bytes")`
  - `page.goto = AsyncMock()`
  - `browser.new_page = AsyncMock(return_value=page)`
  - `browser.close = AsyncMock()`
  - `p.chromium.launch = AsyncMock(return_value=browser)`
- Patch `playwright.async_api.async_playwright` as an async context manager
- Call `result = await tool.run_async(args={}, tool_context=mock_tc)`
- Assert `result["status"] == "success"`
- Assert `result["artifact"] == "verification/latest_snapshot.png"`
- Assert `"Snapshot saved" in result["message"]`
- Assert `mock_tc.save_artifact.called`

**test_run_async_error:**
- Patch playwright import to raise `Exception("Browser crashed")`
- Call `result = await tool.run_async(args={}, tool_context=mock_tc)`
- Assert `result["status"] == "error"`
- Assert `"Could not capture canvas snapshot" in result["message"]`
- Assert `"Browser crashed" in result["message"]`

**test_url_construction:**
- In the success test, verify `page.goto` was called with URL matching `https://graphivac.hvac.io/o/test-org/p/test-project/g/test-grid?iframe=t&init-zoom=t`

Use `@pytest.mark.asyncio` decorator on async tests. Add `pytest.ini` marker config if needed or use `pytest_plugins` for asyncio mode.
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper/agent && python -m pytest tests/test_capture_frontend_state.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at agent/tests/test_capture_frontend_state.py
    - File contains `def test_get_declaration`
    - File contains `async def test_run_async_success`
    - File contains `async def test_run_async_error`
    - File contains `assert result["status"] == "success"`
    - File contains `assert result["status"] == "error"`
    - File contains `"Could not capture canvas snapshot" in result["message"]`
    - All tests pass: `pytest tests/test_capture_frontend_state.py` exits 0
  </acceptance_criteria>
  <done>4 tests pass: declaration check, success path (correct return dict + save_artifact called + URL verified), error path (correct error dict). All mocked, no real Playwright needed.</done>
</task>

</tasks>

<verification>
- `grep "Visual Verification Protocol" agent/master_architecture/prompts/master_instruction.md` matches
- `grep 'capture_frontend_state' agent/master_architecture/prompts/master_instruction.md` matches
- `cd agent && python -m pytest tests/test_capture_frontend_state.py -v` all pass
- `tail -5 agent/master_architecture/prompts/master_instruction.md` shows STOP block is still last
</verification>

<success_criteria>
- master_instruction.md has Visual Verification Protocol with exact 2-step sequence
- 4 unit tests pass covering declaration, success, error, and URL construction
- No changes to any callback, MCP server, or other agent infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/08-implement-capture-frontend-state-visual-verification-tool/08-02-SUMMARY.md`
</output>

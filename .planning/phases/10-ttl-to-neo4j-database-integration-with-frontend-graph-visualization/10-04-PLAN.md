---
phase: 10-ttl-to-neo4j-database-integration-with-frontend-graph-visualization
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-03"]
files_modified: []
autonomous: false
requirements: [P10-01, P10-02, P10-03, P10-04, P10-05]

must_haves:
  truths:
    - "Neo4j starts with Neosemantics plugin loaded"
    - "TTL imports into Neo4j via agent tool and returns correct counts"
    - "GET /api/graph returns nodes and edges JSON"
    - "Graph tab renders interactive Sigma.js visualization with color-coded nodes"
    - "Hover shows info card, click locks card and highlights connections"
---

<objective>
End-to-end human verification of the full Phase 10 pipeline: Neo4j Docker service, TTL import tool, API route, and frontend graph visualization.

Purpose: Confirm the entire vertical slice works as an integrated system.
Output: Human approval of the complete graph visualization feature.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ttl-to-neo4j-database-integration-with-frontend-graph-visualization/10-01-SUMMARY.md
@.planning/phases/10-ttl-to-neo4j-database-integration-with-frontend-graph-visualization/10-02-SUMMARY.md
@.planning/phases/10-ttl-to-neo4j-database-integration-with-frontend-graph-visualization/10-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of Neo4j + Graph tab</name>
  <files>none</files>
  <action>
Human verifies the complete TTL-to-Neo4j-to-frontend pipeline:
1. Neo4j Docker service with Neosemantics plugin (docker-compose graph profile)
2. Python load_ttl_to_neo4j agent tool (wipe + reimport via n10s.rdf.import.inline)
3. GET /api/graph Next.js API route (queries Neo4j, returns nodes/edges JSON)
4. GraphWindow.tsx with Sigma.js WebGL rendering, ForceAtlas2 layout, hover/click interactions, toolbar, info card

**Step 1: Start Neo4j**
```bash
cd /home/juan/codes/si-mapper && docker compose --profile graph up -d neo4j
```
Wait 15-30 seconds for Neo4j to fully start. Verify at http://localhost:7474 (Neo4j Browser should load).

**Step 2: Verify Neosemantics plugin**
Open Neo4j Browser at http://localhost:7474, log in with neo4j / neo4j_password, run:
```cypher
RETURN n10s.graphconfig.show() AS config
```
This should return a result (or error about no config — that is OK, it means n10s is loaded but not yet configured).

**Step 3: Import TTL via agent tool (or manual test)**
If ontology.ttl exists at agent/sub_agents/_223p/ttl/ontology.ttl:
- Use the agent chat to say "load to neo4j" and verify the tool returns success with node/edge counts
- OR run the test manually:
```bash
cd /home/juan/codes/si-mapper/agent && uv run python -c "
import asyncio
from master_architecture.tools.load_ttl_to_neo4j_tool import load_ttl_to_neo4j_tool
result = asyncio.run(load_ttl_to_neo4j_tool.run_async(args={}, tool_context=None))
print(result)
"
```

**Step 4: Verify API route**
```bash
curl -s http://localhost:3000/api/graph | python3 -m json.tool | head -30
```
Should return {"nodes": [...], "edges": [...]} with actual data.

**Step 5: Verify Graph tab in browser**
1. Open the app in browser (http://localhost:3000)
2. Click the "Graph" tab in the navbar
3. Verify: nodes appear on canvas, color-coded by type
4. Verify: ForceAtlas2 layout animates briefly then settles
5. Verify: hover over a node — info card appears at bottom-right
6. Verify: click a node — card locks, connected nodes highlighted, others dim
7. Verify: click elsewhere — selection clears
8. Verify: toolbar buttons work (zoom in/out, reset, FA2 toggle)
9. Verify: zoom in — more labels appear on smaller nodes
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — requires human approval"</automated>
  </verify>
  <done>Human approves all 5 verification steps: Neo4j running, TTL imported, API returns data, graph renders, interactions work</done>
</task>

</tasks>

<verification>
Human confirms all 5 verification steps pass.
</verification>

<success_criteria>
- Neo4j running with n10s plugin
- TTL successfully imported
- API returns graph data
- Graph tab renders interactive visualization
- All interaction patterns work (hover, click, drag, zoom, toolbar)
</success_criteria>

<output>
After completion, create `.planning/phases/10-ttl-to-neo4j-database-integration-with-frontend-graph-visualization/10-04-SUMMARY.md`
</output>

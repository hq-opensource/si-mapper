---
phase: 16-optimize-ontology-skills
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - agent/skills/skill-ontology-generation/SKILL.md
  - agent/skills/skill-ontology-validation/SKILL.md
  - agent/skills/skill-ontology-lessons/SKILL.md
autonomous: true
requirements: [P16-09, P16-10, P16-11]

must_haves:
  truths:
    - "Generation SKILL.md has an Exit Protocol section near the top"
    - "Generation SKILL.md exit call shows no code= parameter"
    - "Validation SKILL.md has Step 0 for lesson loading before the fix loop"
    - "Validation SKILL.md exit call shows no code= or ttl_content= parameters"
    - "Validation SKILL.md does not mention checkpoint_code"
    - "Both SKILL.md files have unambiguous percent operator documentation"
    - "Lessons SKILL.md has unambiguous percent operator lesson"
  artifacts:
    - path: "agent/skills/skill-ontology-generation/SKILL.md"
      provides: "Updated generation workflow with Exit Protocol and clean exit call"
      contains: "## Exit Protocol"
    - path: "agent/skills/skill-ontology-validation/SKILL.md"
      provides: "Updated validation workflow with Step 0 and clean exit call"
      contains: "Step 0"
    - path: "agent/skills/skill-ontology-lessons/SKILL.md"
      provides: "Unambiguous percent operator lesson"
      contains: "sensor % equipment"
  key_links:
    - from: "agent/skills/skill-ontology-generation/SKILL.md"
      to: "agent/tools/ontology_exit_tools.py"
      via: "exit_generator_success(summary=) call instruction"
      pattern: "exit_generator_success\\(summary="
    - from: "agent/skills/skill-ontology-validation/SKILL.md"
      to: "agent/tools/ontology_exit_tools.py"
      via: "exit_validator_success(summary=) call instruction"
      pattern: "exit_validator_success\\(summary="
---

<objective>
Update all three ontology SKILL.md files to match the Wave 2 code changes: clean exit signatures, no checkpoint_code, explicit preparation step, unambiguous percent operator docs, and correct file paths.

Purpose: Agent skills must match the actual tool signatures and behavior. Stale instructions cause the agent to pass removed parameters or call deleted tools, wasting tokens and causing errors.

Output: Updated SKILL.md files for generation, validation, and lessons skills.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-optimize-ontology-skills/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update generation SKILL.md — Exit Protocol, clean exit call, percent operator</name>
  <files>agent/skills/skill-ontology-generation/SKILL.md</files>
  <read_first>
    - agent/skills/skill-ontology-generation/SKILL.md
    - .planning/phases/16-optimize-ontology-skills/16-CONTEXT.md
  </read_first>
  <action>
**A. Add Exit Protocol section** after the "# Workflow" header and before step 1. Insert:
```markdown
## Exit Protocol
The ONLY valid way to signal completion is to call `exit_generator_success(summary="...")` or `exit_generator_failure(reason="...")`.
Do not output any text after calling either tool. The tool call IS your final action.
```

**B. Update Step 10 exit call:**
Change from:
```
10. **Exit** — Call `exit_generator_success(summary="...")` immediately after writing. Summary must include: equipment count, connection types used, notable design decisions. **Do not output text — call the tool.**
```
To:
```
10. **Exit** — Call `exit_generator_success(summary="...")` immediately after writing. No code parameter needed — files are already saved by `write_ontology`. Summary must include: equipment count, connection types used, notable design decisions. **Do not output text — call the tool.**
```

**C. Update percent operator in Connections section:**
Replace the current `%` line:
```
  - `%` (`Sensor.__mod__`): These operators represent sensor-to-equipment relationships.
```
With:
```
  - `%` (`Sensor.__mod__`): Links a sensor to the equipment it monitors — e.g. `temp_sensor % fan`. Do NOT use `%` to attach a measurement property to a sensor — use `sensor.add_property(prop)` for that.
```

**D. Update percent operator in Sensors section:**
Replace the current line:
```
- Use `%` operator exclusively for sensor-to-equipment relationships (do not also set `observes`).
```
With:
```
- `%` links a sensor to the equipment it monitors: `temp_sensor % fan`. Do NOT use `%` to attach a measurement property to a sensor — use `sensor.add_property(prop)` for that. Do not also set `observes`.
```
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper && grep -c "## Exit Protocol" agent/skills/skill-ontology-generation/SKILL.md | grep "^1$" && grep -c "code=" agent/skills/skill-ontology-generation/SKILL.md | grep "^0$" && grep -c "add_property" agent/skills/skill-ontology-generation/SKILL.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Exit Protocol" agent/skills/skill-ontology-generation/SKILL.md` returns 1
    - `grep -c "code=" agent/skills/skill-ontology-generation/SKILL.md` returns 0
    - `grep "add_property" agent/skills/skill-ontology-generation/SKILL.md` finds at least 1 match in the percent operator context
    - `grep "exit_generator_success(summary=" agent/skills/skill-ontology-generation/SKILL.md` finds the clean call
    - Step 10 contains "No code parameter needed"
  </acceptance_criteria>
  <done>Generation SKILL.md has Exit Protocol section, clean exit call without code=, and unambiguous percent operator docs.</done>
</task>

<task type="auto">
  <name>Task 2: Update validation SKILL.md — Step 0, clean exit, remove checkpoint_code, fix paths</name>
  <files>agent/skills/skill-ontology-validation/SKILL.md</files>
  <read_first>
    - agent/skills/skill-ontology-validation/SKILL.md
    - .planning/phases/16-optimize-ontology-skills/16-CONTEXT.md
  </read_first>
  <action>
**A. Add Step 0 — Preparation:**
Change the "**Preparation**" block from:
```
**Preparation**
- Read lessons — Load the skill `skill-ontology-lessons` and apply every error-to-resolution lesson listed there to your generation plan before writing any code. If the file is empty or absent, proceed without it.
```
To a numbered step:
```
**Step 0 — Preparation (run once):** Load skill `skill-ontology-lessons` and apply every lesson to your fixing strategy before entering the loop. If the file is empty or absent, proceed without it.
```

**B. Update Step 2 (execute):**
Change from:
```
2. execute source code — capture stdout, stderr, return code.
```
To:
```
2. Call `execute_ontology()`. Check `result["success"] == true` to determine the path.
```

**C. Update Step 3 (success path):**
Change from:
```
3. No errors + TTL produced → read the TTL file content as a string, then call `exit_validator_success(code=<final_python_code>, ttl_content=<full_ttl_string>, summary="...")` (success summary). The `ttl_content` must be the complete text of the produced `.ttl` file — this is what gets displayed in the frontend TTL tab.
```
To:
```
3. `result["success"] == true` → call `exit_validator_success(summary="...")`. The tool reads the TTL from disk internally — do not pass `code=` or `ttl_content=`.
```

**D. Update Step 4 (fix errors):**
Change from:
```
4. Otherwise: analyze errors → look up affected classes/APIs → write corrected full file via `write_ontology`.
```
To:
```
4. Otherwise: analyze errors → look up affected classes/APIs → fix all errors that share the same root cause in one pass → write corrected full file via `write_ontology`.
```

**E. Delete Step 5 (checkpoint_code):**
Remove entirely:
```
5. After each successful `write_ontology` call, immediately call `checkpoint_code(code=<the_same_full_code_string>)` to save a version snapshot. This is mandatory — do not skip it.
```
Renumber Step 6 to Step 5: `5. Go to step 2.`

**F. Update Fixing Strategy section:**
Change "Fix one error at a time; write the full corrected file after each fix." to:
```
- Fix all errors that share the same root cause in one pass before writing. Write the corrected file once per root cause, not once per error line.
```

**G. Update Available skills and tools section:**
- Remove the `checkpoint_code` tool line entirely: `- \`checkpoint_code\` tool to save a version snapshot after each fix iteration (mandatory after every write_ontology).`
- Update `execute_ontology` line: change "run the current `ontology.py`" to "run `mapper/uploads/python/latest_ontology.py`"
- Update the `exit_validator_success` line to not mention code= or ttl_content=

**H. Fix Role description:**
Change "Read the generated `ontology.py`" to "Read the generated `mapper/uploads/python/latest_ontology.py`"

**I. Add percent operator clarification** in a new "## Operator Reference" section before "## Stop Conditions":
```markdown
## Operator Reference
- `%` links a sensor to the equipment it monitors: `temp_sensor % fan`. Do NOT use `%` to attach a measurement property to a sensor — use `sensor.add_property(prop)` for that.
```
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper && grep -c "checkpoint_code" agent/skills/skill-ontology-validation/SKILL.md | grep "^0$" && grep -c "Step 0" agent/skills/skill-ontology-validation/SKILL.md | grep "^1$" && grep -c "code=" agent/skills/skill-ontology-validation/SKILL.md | grep "^0$" && grep -c "ttl_content=" agent/skills/skill-ontology-validation/SKILL.md | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "checkpoint_code" agent/skills/skill-ontology-validation/SKILL.md` returns 0
    - `grep -c "Step 0" agent/skills/skill-ontology-validation/SKILL.md` returns 1
    - `grep -c "code=" agent/skills/skill-ontology-validation/SKILL.md` returns 0
    - `grep -c "ttl_content=" agent/skills/skill-ontology-validation/SKILL.md` returns 0
    - `grep "exit_validator_success(summary=" agent/skills/skill-ontology-validation/SKILL.md` finds the clean call
    - `grep "latest_ontology.py" agent/skills/skill-ontology-validation/SKILL.md` finds updated paths
    - `grep "add_property" agent/skills/skill-ontology-validation/SKILL.md` finds percent operator clarification
    - `grep "root cause" agent/skills/skill-ontology-validation/SKILL.md` finds batching instruction
  </acceptance_criteria>
  <done>Validation SKILL.md has Step 0, clean exit signature, no checkpoint_code, correct paths, batching strategy, and percent operator docs.</done>
</task>

<task type="auto">
  <name>Task 3: Update lessons SKILL.md — unambiguous percent operator lesson</name>
  <files>agent/skills/skill-ontology-lessons/SKILL.md</files>
  <read_first>
    - agent/skills/skill-ontology-lessons/SKILL.md
  </read_first>
  <action>
In the **Sensor API** section, replace the second bullet:
```
- **Error:** Using the `%` operator to associate a property with a sensor (`sensor % property`). -> **Fix:** Use the `add_property()` method: `sensor.add_property(property)`.
```
With:
```
- **Error:** Using `sensor % property` to attach a measurement property to a sensor. -> **Fix:** Use `sensor.add_property(prop)`. Note: `sensor % equipment` (attaching a sensor to the thing it monitors) is correct — only `sensor % property` is wrong.
```
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper && grep "sensor % equipment" agent/skills/skill-ontology-lessons/SKILL.md | grep -c "correct" | grep "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sensor % equipment" agent/skills/skill-ontology-lessons/SKILL.md` finds the clarification
    - `grep "sensor % property" agent/skills/skill-ontology-lessons/SKILL.md` finds the error description
    - The lesson explicitly states that `sensor % equipment` is correct
    - The lesson explicitly states that `sensor % property` is wrong
  </acceptance_criteria>
  <done>Lessons SKILL.md has unambiguous percent operator lesson that distinguishes correct (sensor % equipment) from incorrect (sensor % property) usage.</done>
</task>

</tasks>

<verification>
- `grep -rn "checkpoint_code" agent/skills/` returns 0 matches
- `grep -rn "code=" agent/skills/skill-ontology-generation/SKILL.md agent/skills/skill-ontology-validation/SKILL.md` returns 0 matches
- All three SKILL.md files are valid markdown (no broken formatting)
</verification>

<success_criteria>
- Generation SKILL.md: Exit Protocol section present, no code= in exit call, unambiguous % docs
- Validation SKILL.md: Step 0 present, no checkpoint_code, no code=/ttl_content= in exit call, correct paths, batching strategy, % docs
- Lessons SKILL.md: % lesson distinguishes sensor%equipment (correct) from sensor%property (wrong)
</success_criteria>

<output>
After completion, create `.planning/phases/16-optimize-ontology-skills/16-03-SUMMARY.md`
</output>

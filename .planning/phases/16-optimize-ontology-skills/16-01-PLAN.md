---
phase: 16-optimize-ontology-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/tools/ontology_exit_tools.py
  - agent/tools/ontology_tools.py
autonomous: true
requirements: [P16-01, P16-02]

must_haves:
  truths:
    - "_persist_python and _persist_ttl functions no longer exist in ontology_exit_tools.py"
    - "ontology_tools.py does not import _persist_python or _persist_ttl"
    - "write_ontology and execute_ontology still write to both expected locations without the deleted helpers"
  artifacts:
    - path: "agent/tools/ontology_exit_tools.py"
      provides: "Exit tools without _persist_python, _persist_ttl, checkpoint_code"
      contains: "exit_generator_success"
    - path: "agent/tools/ontology_tools.py"
      provides: "Ontology tools without _persist imports"
  key_links:
    - from: "agent/tools/ontology_tools.py"
      to: "agent/tools/ontology_exit_tools.py"
      via: "no import of _persist_python or _persist_ttl"
      pattern: "^(?!.*_persist)"
---

<objective>
Delete `_persist_python` and `_persist_ttl` helper functions and remove all their call sites.

Purpose: These helpers write redundant timestamped copies to mapper/uploads/ when write_ontology and execute_ontology already handle persistence via the two-folder structure (uploads + session archives). Removing them eliminates duplicate file writes.

Output: Clean ontology_exit_tools.py and ontology_tools.py with no _persist references.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-optimize-ontology-skills/16-CONTEXT.md

<interfaces>
<!-- Current signatures and imports the executor needs to know about -->

From agent/tools/ontology_exit_tools.py (lines 22-47):
```python
def _persist_python(code: str, label: str) -> None:
def _persist_ttl(ttl_content: str) -> None:
```
These two functions are being DELETED entirely.

From agent/tools/ontology_tools.py (line 30):
```python
from tools.ontology_exit_tools import _persist_python, _persist_ttl
```
This import line is being DELETED.

Call sites to remove:
- ontology_exit_tools.py line 85: `_persist_python(code, "exit_generator_success")`
- ontology_exit_tools.py line 138: `_persist_ttl(ttl_content)`
- ontology_exit_tools.py line 139: `_persist_python(code, "exit_validator_success")`
- ontology_tools.py line 345: `_persist_ttl(ttl_content)` inside execute_ontology
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete _persist_python and _persist_ttl from ontology_exit_tools.py and remove call sites in that file</name>
  <files>agent/tools/ontology_exit_tools.py</files>
  <read_first>
    - agent/tools/ontology_exit_tools.py
  </read_first>
  <action>
1. Delete the `_persist_python` function (lines 22-33) entirely.
2. Delete the `_persist_ttl` function (lines 36-47) entirely.
3. Remove the `from datetime import datetime` import (only used by deleted functions).
4. Remove the `from pathlib import Path` import (only used by deleted functions for `_UPLOADS_PYTHON` and `_UPLOADS_TTL`).
5. Delete the `_UPLOADS_PYTHON` constant (line 18).
6. Delete the `_UPLOADS_TTL` constant (line 19).
7. In `exit_generator_success`: remove the line `_persist_python(code, "exit_generator_success")` (line 85).
8. In `exit_validator_success`: remove `_persist_ttl(ttl_content)` (line 138) and `_persist_python(code, "exit_validator_success")` (line 139).

Do NOT change function signatures yet (code= removal is Wave 2). Only remove the deleted helper calls and definitions.

Note: Keep `from pathlib import Path` ONLY if it is used elsewhere in the file. Check — it is NOT used elsewhere, so delete it.
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper && grep -c "_persist_python\|_persist_ttl\|_UPLOADS_PYTHON\|_UPLOADS_TTL" agent/tools/ontology_exit_tools.py | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_persist_python" agent/tools/ontology_exit_tools.py` returns 0
    - `grep -c "_persist_ttl" agent/tools/ontology_exit_tools.py` returns 0
    - `grep -c "_UPLOADS_PYTHON" agent/tools/ontology_exit_tools.py` returns 0
    - `grep -c "_UPLOADS_TTL" agent/tools/ontology_exit_tools.py` returns 0
    - `grep -c "from datetime import datetime" agent/tools/ontology_exit_tools.py` returns 0
    - `exit_generator_success` function still exists
    - `exit_validator_success` function still exists
    - `checkpoint_code` function still exists (removed in Wave 2)
  </acceptance_criteria>
  <done>_persist_python, _persist_ttl, and all their constants and call sites are deleted from ontology_exit_tools.py. Remaining functions are unchanged except for the removed calls.</done>
</task>

<task type="auto">
  <name>Task 2: Remove _persist import from ontology_tools.py and clean up _persist_ttl call site in execute_ontology</name>
  <files>agent/tools/ontology_tools.py</files>
  <read_first>
    - agent/tools/ontology_tools.py
  </read_first>
  <action>
1. Delete line 30: `from tools.ontology_exit_tools import _persist_python, _persist_ttl`
2. In `execute_ontology`, remove the `_persist_ttl(ttl_content)` call at line 345. The TTL is already written to `latest_ontology.ttl` by the script itself (the subprocess writes it to TTL_OUTPUT_DIR as its cwd), and the session archive write on lines 367-373 handles versioning. No replacement needed.
3. Verify no other references to `_persist_python` or `_persist_ttl` remain in the file.
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper && grep -c "_persist_python\|_persist_ttl\|from tools.ontology_exit_tools" agent/tools/ontology_tools.py | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_persist_python" agent/tools/ontology_tools.py` returns 0
    - `grep -c "_persist_ttl" agent/tools/ontology_tools.py` returns 0
    - `grep -c "from tools.ontology_exit_tools" agent/tools/ontology_tools.py` returns 0
    - `write_ontology` function still writes to ONTOLOGY_FILE (primary) and session archive
    - `execute_ontology` function still writes session archive for TTL
  </acceptance_criteria>
  <done>ontology_tools.py has no imports from ontology_exit_tools.py. No _persist calls remain. Two-write pattern is preserved (primary + session archive).</done>
</task>

</tasks>

<verification>
- `grep -rn "_persist_python\|_persist_ttl" agent/tools/` returns no matches
- `python -c "import sys; sys.path.insert(0,'agent'); exec(open('agent/tools/ontology_tools.py').read())"` does not raise ImportError (no longer imports from ontology_exit_tools)
</verification>

<success_criteria>
- Zero references to `_persist_python` or `_persist_ttl` in the entire `agent/tools/` directory
- Both `write_ontology` and `execute_ontology` still write to their expected locations (ONTOLOGY_FILE, session archives, TTL_OUTPUT_DIR)
- No import of `_persist_python` or `_persist_ttl` anywhere in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/16-optimize-ontology-skills/16-01-SUMMARY.md`
</output>

---
phase: 15-refactor-coding-skills-and-standardize-agent-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/tests/test_create_master_agent.py
  - agent/tests/test_223p_tools.py (DELETE)
  - agent/tests/test_ontology_generator_agent.py (DELETE)
  - agent/tests/test_ontology_generator_exit_tools.py (DELETE)
  - agent/tests/test_ontology_validator_agent.py (DELETE)
  - agent/tests/test_ontology_validator_exit_tools.py (DELETE)
  - agent/sub_agents/ (DELETE entire directory)
autonomous: true
requirements:
  - P15-01
  - P15-02

must_haves:
  truths:
    - "agent/sub_agents/ directory does not exist"
    - "No Python file in agent/ imports from sub_agents"
    - "test_create_master_agent.py has no assertions checking for sub_agents import strings"
    - "All remaining tests pass (no broken imports)"
  artifacts:
    - path: "agent/tests/test_create_master_agent.py"
      provides: "Updated test file with stale assertions removed"
      contains: "test_imports_adapted_exit_tools"
  key_links:
    - from: "agent/master_architecture/create_master_agent.py"
      to: "agent/tools/ontology_tools.py"
      via: "direct import (no sub_agents path)"
      pattern: "from tools\\.ontology_tools import"
---

<objective>
Delete the entire agent/sub_agents/ directory (dead code after Phase 13 migration) and clean up all test files that exclusively test sub_agents code.

Purpose: Remove dead code that creates confusion about the canonical tool locations and wastes repository space. The master agent imports zero things from sub_agents — deletion is clean.
Output: sub_agents/ gone, 5 test files deleted, test_create_master_agent.py updated with 2 stale assertions removed.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-refactor-coding-skills-and-standardize-agent-architecture/15-CONTEXT.md

<interfaces>
<!-- Key imports in create_master_agent.py — these must NOT break -->
From agent/master_architecture/create_master_agent.py:
```python
from tools.ontology_tools import (
    scan_python_files_filtered,
    search_class_mapping,
    write_ontology,
    read_ontology,
    execute_ontology,
    read_prompt,
)
from tools.ontology_exit_tools import (
    checkpoint_code,
    exit_generator_success,
    exit_generator_failure,
    exit_validator_success,
    exit_validator_failure,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete sub_agents directory and stale test files</name>
  <files>
    agent/sub_agents/ (DELETE)
    agent/tests/test_223p_tools.py (DELETE)
    agent/tests/test_ontology_generator_agent.py (DELETE)
    agent/tests/test_ontology_generator_exit_tools.py (DELETE)
    agent/tests/test_ontology_validator_agent.py (DELETE)
    agent/tests/test_ontology_validator_exit_tools.py (DELETE)
    agent/tests/test_create_master_agent.py (MODIFY)
  </files>
  <read_first>
    agent/tests/test_create_master_agent.py
    agent/master_architecture/create_master_agent.py
  </read_first>
  <action>
1. Verify no file in agent/ (outside sub_agents/) imports from sub_agents:
   Run: `cd agent && grep -r "from sub_agents" --include="*.py" . | grep -v "./sub_agents/" | grep -v __pycache__`
   Expected: zero results (master already imports from tools/).

2. Delete the entire agent/sub_agents/ directory:
   `rm -rf agent/sub_agents/`

3. Delete exactly these 5 test files:
   - `rm agent/tests/test_223p_tools.py`
   - `rm agent/tests/test_ontology_generator_agent.py`
   - `rm agent/tests/test_ontology_generator_exit_tools.py`
   - `rm agent/tests/test_ontology_validator_agent.py`
   - `rm agent/tests/test_ontology_validator_exit_tools.py`

4. Edit agent/tests/test_create_master_agent.py — remove exactly these two test functions:
   - `test_imports_ontology_tools_from_223p` (lines 52-58) — asserts `"from sub_agents._223p.tool import" in src` which is stale
   - `test_imports_checkpoint_code_from_validator` (lines 61-64) — asserts `"from sub_agents.ontology_validator.exit_tools import checkpoint_code" in src` which is stale

   Keep all other test functions intact:
   - test_no_ontology_generator_agent_import
   - test_no_ontology_validator_agent_import
   - test_no_ontology_subagents_list
   - test_imports_adapted_exit_tools
   - test_max_iterations_is_100
   - test_main_py_does_not_import_sequential_agent
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper/agent && test ! -d sub_agents && echo "sub_agents deleted" && .venv/bin/python -m pytest tests/test_create_master_agent.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `agent/sub_agents/` directory does not exist: `test ! -d agent/sub_agents && echo PASS`
    - `agent/tests/test_223p_tools.py` does not exist: `test ! -f agent/tests/test_223p_tools.py && echo PASS`
    - `agent/tests/test_ontology_generator_agent.py` does not exist
    - `agent/tests/test_ontology_generator_exit_tools.py` does not exist
    - `agent/tests/test_ontology_validator_agent.py` does not exist
    - `agent/tests/test_ontology_validator_exit_tools.py` does not exist
    - `test_create_master_agent.py` does NOT contain `test_imports_ontology_tools_from_223p`: `grep -c "test_imports_ontology_tools_from_223p" agent/tests/test_create_master_agent.py` returns 0
    - `test_create_master_agent.py` does NOT contain `test_imports_checkpoint_code_from_validator`: `grep -c "test_imports_checkpoint_code_from_validator" agent/tests/test_create_master_agent.py` returns 0
    - `test_create_master_agent.py` DOES contain `test_imports_adapted_exit_tools`: `grep -c "test_imports_adapted_exit_tools" agent/tests/test_create_master_agent.py` returns 1
    - All 6 remaining tests pass: `cd agent && .venv/bin/python -m pytest tests/test_create_master_agent.py -v` exits 0
    - No file outside sub_agents/ imports from sub_agents: `cd agent && grep -r "from sub_agents" --include="*.py" . | grep -v __pycache__` returns empty
  </acceptance_criteria>
  <done>agent/sub_agents/ deleted, 5 stale test files deleted, test_create_master_agent.py has 6 passing tests with 0 sub_agents references</done>
</task>

</tasks>

<verification>
- `test ! -d agent/sub_agents && echo "PASS: sub_agents deleted"`
- `cd agent && .venv/bin/python -m pytest tests/test_create_master_agent.py -v` — 6 tests pass
- `cd agent && grep -r "from sub_agents" --include="*.py" . | grep -v __pycache__ | wc -l` — returns 0
</verification>

<success_criteria>
- agent/sub_agents/ directory completely removed
- 5 dead test files removed
- test_create_master_agent.py updated: 2 stale assertions removed, 6 remaining tests pass
- No Python file in agent/ references sub_agents
</success_criteria>

<output>
After completion, create `.planning/phases/15-refactor-coding-skills-and-standardize-agent-architecture/15-01-SUMMARY.md`
</output>

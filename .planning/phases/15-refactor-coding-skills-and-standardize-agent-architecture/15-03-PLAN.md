---
phase: 15-refactor-coding-skills-and-standardize-agent-architecture
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
files_modified:
  - agent/skills/skill-ontology-generation/SKILL.md
  - agent/skills/skill-ontology-validation/SKILL.md
autonomous: true
requirements:
  - P15-08
  - P15-09
  - P15-10

must_haves:
  truths:
    - "skill-ontology-generation has Step 0 reading LESSONS.md from agent/223p/LESSONS.md"
    - "skill-ontology-generation references agent/223p/ref/code (not ../223p/ref/code)"
    - "skill-ontology-generation includes extract_lessons HITL instruction"
    - "skill-ontology-generation includes BACnet custom_fields awareness"
    - "skill-ontology-generation does not reference skill-read-code"
    - "skill-ontology-validation does not reference skill-read-code"
    - "skill-ontology-validation references correct paths for agent/223p/"
    - "skill-ontology-validation references read_prompt tool with correct path context"
  artifacts:
    - path: "agent/skills/skill-ontology-generation/SKILL.md"
      provides: "Enhanced generation skill with LESSONS.md reading, extract_lessons, BACnet awareness, fixed paths"
      contains: "agent/223p/LESSONS.md"
    - path: "agent/skills/skill-ontology-validation/SKILL.md"
      provides: "Enhanced validation skill with fixed paths, no skill-read-code references"
  key_links:
    - from: "agent/skills/skill-ontology-generation/SKILL.md"
      to: "agent/223p/LESSONS.md"
      via: "Step 0 instruction to read LESSONS.md"
      pattern: "agent/223p/LESSONS\\.md"
    - from: "agent/skills/skill-ontology-generation/SKILL.md"
      to: "agent/tools/ontology_tools.py"
      via: "extract_lessons tool reference"
      pattern: "extract_lessons"
    - from: "agent/skills/skill-ontology-validation/SKILL.md"
      to: "agent/tools/ontology_tools.py"
      via: "read_prompt tool reference"
      pattern: "read_prompt"
---

<objective>
Audit and enhance both ontology skills (generation and validation) to fix path references, embed LESSONS.md reading, add extract_lessons HITL instruction, add BACnet metadata awareness, and remove all references to the deleted skill-read-code.

Purpose: Skills are the LLM's working instructions. Stale paths and references to deleted skills cause silent failures during ontology generation/validation. This plan ensures both skills reference correct locations and leverage the new tools.
Output: Both SKILL.md files updated, audited, and verified.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-refactor-coding-skills-and-standardize-agent-architecture/15-CONTEXT.md
@.planning/phases/15-refactor-coding-skills-and-standardize-agent-architecture/15-02-SUMMARY.md

<interfaces>
<!-- Current skill-ontology-generation/SKILL.md issues to fix -->
Line 30: `scan_python_files_filtered` on `../223p/ref/code` -- WRONG path after migration, must be `agent/223p/ref/code`
Line 32: "Use the `skill-read-code` skill to acquire error-resolution lessons." -- MUST DELETE, skill-read-code no longer exists
No Step 0 for LESSONS.md reading -- MUST ADD
No BACnet custom_fields awareness -- MUST ADD
No extract_lessons instruction -- MUST ADD

<!-- Current skill-ontology-validation/SKILL.md issues to fix -->
Line 17: "Use `skill-read-code` skill to acquire error-resolution lessons." -- MUST DELETE
Line 37: "`skill-read-code` skill" in Available tools section -- MUST DELETE
Line 39: `../223p/ref/code` reference -- MUST FIX to `agent/223p/ref/code`
Line 51: "Usage of skill `skill-read-code` was unsuccessfully" stop condition -- MUST REWRITE

<!-- Available tools the skills can reference (from ontology_tools.py post-Plan 02) -->
- scan_python_files_filtered(path, keywords) -- scan .py files
- search_class_mapping(keywords) -- grep JSONL class mappings
- read_ontology() -- read agent/223p/ontology.py
- write_ontology(content) -- three-write pattern
- execute_ontology() -- run ontology.py, three-write for TTL
- read_prompt() -- read agent/223p/ref/code/prompt.md
- extract_lessons() -- read all session iterations (HITL-gated)
- checkpoint_code(code) -- save version snapshot
- exit_generator_success(code, summary)
- exit_generator_failure(reason)
- exit_validator_success(code, summary, ttl_content)
- exit_validator_failure(reason)
- read_internal_grid() -- read HVAC components from state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and rewrite skill-ontology-generation/SKILL.md</name>
  <files>agent/skills/skill-ontology-generation/SKILL.md</files>
  <read_first>
    agent/skills/skill-ontology-generation/SKILL.md
    agent/tools/ontology_tools.py
    agent/223p/LESSONS.md
  </read_first>
  <action>
Read the current SKILL.md in full. Apply these specific changes (methodology: read-then-change per CONTEXT.md):

**1. Add Step 0 — Read Lessons (BEFORE Workflow section, as the very first workflow step):**

Insert before the current Step 1 in the Workflow section:
```markdown
0. **Lessons** — Read `agent/223p/LESSONS.md` using `scan_python_files_filtered(path="agent/223p", keywords=["LESSONS"])` or ask the LLM to read the file directly. If the file is empty or absent, proceed without it. Do NOT use `skill-read-code` — that skill no longer exists. Apply any error-resolution lessons to your generation plan.
```

**2. Fix path reference in Step 4 (currently referencing ../223p/ref/code):**

Change:
```
Run `scan_python_files_filtered` on `../223p/ref/code`
```
To:
```
Run `scan_python_files_filtered` on `agent/223p/ref/code`
```

**3. Delete skill-read-code reference (line 32):**

Remove the entire line:
```
- Use the `skill-read-code` skill to acquire error-resolution lessons.
```

**4. Add extract_lessons HITL instruction (new section after Workflow):**

Add a new section:
```markdown
# Lesson Extraction (HITL gate)

If the user explicitly says "extract lessons", "update lessons", or "distill lessons":
1. Call `extract_lessons()` tool — it returns all Python iteration files across sessions.
2. Analyze the iteration sequence: identify what patterns caused errors in earlier versions and how later versions fixed them.
3. Write the updated `agent/223p/LESSONS.md` using `write_ontology` or direct file write, structured by the 6 categories: Imports, Instantiation pattern, Connection wiring, Sensor API, Serialization, Structural approach.
4. Do NOT auto-trigger this step. Only execute when the user explicitly asks.
```

**5. Add BACnet metadata awareness (in the Equipment section under Modeling Guidelines):**

Add this bullet after the existing equipment bullet:
```markdown
- Components returned by `read_internal_grid` may have a `custom_fields` key containing BACnet points (object names, descriptions, units). When modeling sensors and equipment, include these BACnet metadata points as properties in the ontology — they provide real-world measurement context.
```

**6. Renumber workflow steps** — with Step 0 added, the existing steps 1-9 become 0-9 (10 steps total). Renumber accordingly.

**7. Full audit** — verify every tool name referenced in the skill actually exists in the tools list:
- `read_internal_grid` -- EXISTS (internal_grid_tools.py)
- `search_class_mapping` -- EXISTS (ontology_tools.py)
- `scan_python_files_filtered` -- EXISTS (ontology_tools.py)
- `write_ontology` -- EXISTS (ontology_tools.py)
- `exit_generator_success` -- EXISTS (ontology_exit_tools.py)
- `exit_generator_failure` -- EXISTS (ontology_exit_tools.py)
- `extract_lessons` -- EXISTS (ontology_tools.py, added in Plan 02)
  </action>
  <verify>
    <automated>grep -c "agent/223p/LESSONS.md" /home/juan/codes/si-mapper/agent/skills/skill-ontology-generation/SKILL.md && grep -c "skill-read-code" /home/juan/codes/si-mapper/agent/skills/skill-ontology-generation/SKILL.md && grep -c "agent/223p/ref/code" /home/juan/codes/si-mapper/agent/skills/skill-ontology-generation/SKILL.md && grep -c "extract_lessons" /home/juan/codes/si-mapper/agent/skills/skill-ontology-generation/SKILL.md && grep -c "custom_fields" /home/juan/codes/si-mapper/agent/skills/skill-ontology-generation/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains "agent/223p/LESSONS.md": `grep -c "agent/223p/LESSONS.md" agent/skills/skill-ontology-generation/SKILL.md` returns >= 1
    - SKILL.md does NOT contain "skill-read-code": `grep -c "skill-read-code" agent/skills/skill-ontology-generation/SKILL.md` returns 0
    - SKILL.md contains "agent/223p/ref/code" (not "../223p/ref/code"): `grep -c "agent/223p/ref/code" agent/skills/skill-ontology-generation/SKILL.md` returns >= 1
    - SKILL.md does NOT contain "../223p/ref/code": `grep -c "\\.\\./223p/ref/code" agent/skills/skill-ontology-generation/SKILL.md` returns 0
    - SKILL.md contains "extract_lessons": `grep -c "extract_lessons" agent/skills/skill-ontology-generation/SKILL.md` returns >= 1
    - SKILL.md contains "custom_fields" (BACnet awareness): `grep -c "custom_fields" agent/skills/skill-ontology-generation/SKILL.md` returns >= 1
    - SKILL.md contains "HITL" or "user explicitly": `grep -c "user explicitly" agent/skills/skill-ontology-generation/SKILL.md` returns >= 1
    - SKILL.md still contains "exit_generator_success": `grep -c "exit_generator_success" agent/skills/skill-ontology-generation/SKILL.md` returns >= 1
    - SKILL.md still contains "write_ontology": `grep -c "write_ontology" agent/skills/skill-ontology-generation/SKILL.md` returns >= 1
  </acceptance_criteria>
  <done>skill-ontology-generation SKILL.md fully updated: LESSONS.md Step 0 added, paths fixed, skill-read-code removed, extract_lessons instruction added, BACnet awareness added, all tool references verified</done>
</task>

<task type="auto">
  <name>Task 2: Audit and rewrite skill-ontology-validation/SKILL.md</name>
  <files>agent/skills/skill-ontology-validation/SKILL.md</files>
  <read_first>
    agent/skills/skill-ontology-validation/SKILL.md
    agent/tools/ontology_tools.py
  </read_first>
  <action>
Read the current SKILL.md in full. Apply these specific changes:

**1. Remove Preparation step referencing skill-read-code:**

Delete:
```markdown
**Preparation**
- Use `skill-read-code` skill to acquire error-resolution lessons.  Confirm when done.
```

Replace with:
```markdown
**Preparation**
- Read `agent/223p/LESSONS.md` for error-resolution lessons from previous sessions. If the file is empty or absent, proceed without it.
- Use `read_prompt` to read the original generation guidelines from `agent/223p/ref/code/prompt.md`. Keep result in cache.
```

**2. Fix Available skills and tools section:**

Remove:
```markdown
- `skill-read-code` skill to acquire error-resolution lessons. Keep result in cache.
```

Replace with:
```markdown
- Read `agent/223p/LESSONS.md` directly for error-resolution lessons (no separate skill needed).
```

Fix path reference:
Change `../223p/ref/code` to `agent/223p/ref/code` in the `scan_python_files_filtered` bullet.

**3. Fix Stop Conditions:**

Remove:
```markdown
- Usage of skill `skill-read-code` was unsuccessfully — unable to acquire error-resolution lessons.
```

Replace with:
```markdown
- `agent/223p/LESSONS.md` is unreadable and no error-resolution context is available — proceed with best-effort fixing but log the limitation.
```

**4. Full audit** — verify every tool name referenced in the skill actually exists:
- `search_class_mapping` -- EXISTS (ontology_tools.py)
- `scan_python_files_filtered` -- EXISTS (ontology_tools.py)
- `read_prompt` -- EXISTS (ontology_tools.py, reads from agent/223p/ref/code/prompt.md after Plan 02)
- `read_ontology` -- EXISTS (ontology_tools.py)
- `execute_ontology` -- EXISTS (ontology_tools.py)
- `write_ontology` -- EXISTS (ontology_tools.py)
- `checkpoint_code` -- EXISTS (ontology_exit_tools.py)
- `exit_validator_success` -- EXISTS (ontology_exit_tools.py)
- `exit_validator_failure` -- EXISTS (ontology_exit_tools.py)

**5. Verify workflow steps** are internally consistent:
- Step 1: read_ontology -- correct
- Step 2: execute_ontology -- correct
- Step 3: exit_validator_success with ttl_content -- correct
- Step 4: write_ontology + checkpoint_code -- correct
- All steps reference existing tools with correct parameter names
  </action>
  <verify>
    <automated>grep -c "skill-read-code" /home/juan/codes/si-mapper/agent/skills/skill-ontology-validation/SKILL.md && grep -c "agent/223p/LESSONS.md" /home/juan/codes/si-mapper/agent/skills/skill-ontology-validation/SKILL.md && grep -c "agent/223p/ref/code" /home/juan/codes/si-mapper/agent/skills/skill-ontology-validation/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md does NOT contain "skill-read-code": `grep -c "skill-read-code" agent/skills/skill-ontology-validation/SKILL.md` returns 0
    - SKILL.md contains "agent/223p/LESSONS.md": `grep -c "agent/223p/LESSONS.md" agent/skills/skill-ontology-validation/SKILL.md` returns >= 1
    - SKILL.md does NOT contain "../223p/ref/code": `grep -c "\\.\\./223p/ref/code" agent/skills/skill-ontology-validation/SKILL.md` returns 0
    - SKILL.md contains "agent/223p/ref/code": `grep -c "agent/223p/ref/code" agent/skills/skill-ontology-validation/SKILL.md` returns >= 1
    - SKILL.md still contains "exit_validator_success": `grep -c "exit_validator_success" agent/skills/skill-ontology-validation/SKILL.md` returns >= 1
    - SKILL.md still contains "checkpoint_code": `grep -c "checkpoint_code" agent/skills/skill-ontology-validation/SKILL.md` returns >= 1
    - SKILL.md still contains "write_ontology": `grep -c "write_ontology" agent/skills/skill-ontology-validation/SKILL.md` returns >= 1
    - SKILL.md still contains "read_prompt": `grep -c "read_prompt" agent/skills/skill-ontology-validation/SKILL.md` returns >= 1
  </acceptance_criteria>
  <done>skill-ontology-validation SKILL.md fully updated: skill-read-code references removed, paths fixed to agent/223p/, preparation step updated, all tool references verified</done>
</task>

</tasks>

<verification>
- `grep -r "skill-read-code" agent/skills/ | wc -l` — returns 0 (no references to deleted skill anywhere)
- `grep -r "\.\./223p" agent/skills/ | wc -l` — returns 0 (no relative paths to old root 223p/)
- `grep -c "agent/223p" agent/skills/skill-ontology-generation/SKILL.md` — returns >= 3
- `grep -c "agent/223p" agent/skills/skill-ontology-validation/SKILL.md` — returns >= 2
- Full test suite: `cd agent && .venv/bin/python -m pytest tests/ -v --ignore=tests/test_capture_frontend_state_live.py --ignore=tests/test_write_metadata_batch_live.py` — all tests pass
</verification>

<success_criteria>
- Both SKILL.md files have zero references to skill-read-code
- Both SKILL.md files use agent/223p/ paths (not ../223p/ or sub_agents/)
- skill-ontology-generation has LESSONS.md Step 0, extract_lessons instruction, BACnet awareness
- skill-ontology-validation has updated preparation step, fixed stop conditions
- All tool references in both skills map to actually existing tools
</success_criteria>

<output>
After completion, create `.planning/phases/15-refactor-coding-skills-and-standardize-agent-architecture/15-03-SUMMARY.md`
</output>

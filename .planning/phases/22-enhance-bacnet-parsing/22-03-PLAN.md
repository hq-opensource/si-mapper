---
phase: 22-enhance-bacnet-parsing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/skills/skill-ontology-generation/SKILL.md
  - agent/skills/skill-ontology-validation/SKILL.md
  - agent/skills/skill-ontology-lessons/SKILL.md
autonomous: true
requirements: [P22-03]
must_haves:
  truths:
    - "Ontology generation skill instructs agents to use pre-computed bacnet_N.address URI directly instead of parsing raw addresses"
    - "Ontology validation skill instructs agents to check for pre-formed bacnet:// URIs and use them directly"
    - "Ontology lessons skill documents the new dictionary format (code, address URI, ref_type) and notes the old parsing rules as legacy fallback"
  artifacts:
    - path: "agent/skills/skill-ontology-generation/SKILL.md"
      provides: "Updated BACnet External References section"
      contains: "bacnet_N.address"
    - path: "agent/skills/skill-ontology-validation/SKILL.md"
      provides: "Updated BACnet External References section"
      contains: "bacnet_N.address"
    - path: "agent/skills/skill-ontology-lessons/SKILL.md"
      provides: "Updated BACnet External References lesson with new format"
      contains: "ref_type"
  key_links: []
---

<objective>
Update three ontology skill SKILL.md files to document the new pre-computed BACnet dictionary format (code, address URI, ref_type) so ontology agents consume enriched fields directly instead of manually parsing raw addresses.

Purpose: Eliminate manual address parsing from agent prompts — the Python layer now pre-computes everything.
Output: Three updated SKILL.md files with correct field references.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-enhance-bacnet-parsing/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skill-ontology-generation SKILL.md BACnet section</name>
  <files>agent/skills/skill-ontology-generation/SKILL.md</files>
  <read_first>
    - agent/skills/skill-ontology-generation/SKILL.md (full file — find "BACnet External References" section, currently ~line 64-65)
  </read_first>
  <action>
Replace the "## BACnet External References" section (currently 2 lines: the header and a single line referencing skill-ontology-lessons for address-parsing rules) with:

```markdown
## BACnet External References
When components have `custom_fields` with `bacnet_N` entries, **always create `BACnetExternalReference` objects** — do not store addresses only as comments.

Each `bacnet_N` entry contains pre-computed fields:
- `code` — the original raw BACnet address (e.g. `"2500.AI13"`)
- `address` — the fully-formed BACnet URI (e.g. `"bacnet://2500/analog-input,13/present-value"`) or `null` for skip types
- `ref_type` — `"sensor"`, `"property"`, or `"skip"`

Use `bacnet_N["address"]` directly when constructing `BACnetExternalReference`. Skip entries where `ref_type == "skip"` or `address` is `null`. No manual address parsing is needed.

See `skill-ontology-lessons` (section "BACnet External References") for code patterns and the `@` operator usage.
```

Do NOT change any other section of the file.
  </action>
  <verify>
    <automated>grep -c "ref_type" agent/skills/skill-ontology-generation/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ref_type" agent/skills/skill-ontology-generation/SKILL.md returns at least 2
    - grep -c "No manual address parsing" agent/skills/skill-ontology-generation/SKILL.md returns 1
    - grep -c 'bacnet_N\["address"\]' agent/skills/skill-ontology-generation/SKILL.md returns 1
    - grep "address-parsing rules, type suffix map" agent/skills/skill-ontology-generation/SKILL.md returns NO match (old text removed)
  </acceptance_criteria>
  <done>Generation skill documents pre-computed fields and instructs agents to use address URI directly.</done>
</task>

<task type="auto">
  <name>Task 2: Update skill-ontology-validation and skill-ontology-lessons SKILL.md</name>
  <files>agent/skills/skill-ontology-validation/SKILL.md, agent/skills/skill-ontology-lessons/SKILL.md</files>
  <read_first>
    - agent/skills/skill-ontology-validation/SKILL.md (full file — find "BACnet External References" section, currently ~line 100-101)
    - agent/skills/skill-ontology-lessons/SKILL.md (full file — find "BACnet External References" section with address parsing lesson, currently ~line 35-48)
  </read_first>
  <action>
**skill-ontology-validation/SKILL.md:**

Replace the "## BACnet External References" section (currently 2 lines: header + instruction referencing skill-ontology-lessons) with:

```markdown
## BACnet External References
If the ontology contains `comment="BACnet: ..."` patterns but no `BACnetExternalReference` imports or usages, treat this as a fixable issue.

Each `bacnet_N` entry in `custom_fields` contains pre-computed fields:
- `code` — the original raw BACnet address (e.g. `"2500.AI13"`)
- `address` — the fully-formed BACnet URI (e.g. `"bacnet://2500/analog-input,13/present-value"`) or `null` for skip types
- `ref_type` — `"sensor"`, `"property"`, or `"skip"`

Use `bacnet_N["address"]` directly when constructing `BACnetExternalReference`. Skip entries where `ref_type == "skip"` or `address` is `null`. No manual address parsing is needed.

See `skill-ontology-lessons` (section "BACnet External References") for the `@` operator patterns.
```

**skill-ontology-lessons/SKILL.md:**

In the "## BACnet External References" section, keep ALL existing content (error description, code patterns for sensors and equipment, validation rule) but ADD a new paragraph at the very TOP of the section content (right after the `## BACnet External References` header), BEFORE the existing "- **Error:**" bullet:

```markdown
> **Phase 22 update:** Each `bacnet_N` entry now contains pre-computed fields: `code` (original raw address), `address` (fully-formed BACnet URI or `null` for skip types), and `ref_type` (`"sensor"`, `"property"`, or `"skip"`). Use `bacnet_N["address"]` directly — the manual parsing rules below are retained as legacy fallback only.

```

Do NOT delete any existing content in the lessons file. The existing address parsing rules, suffix map, and code patterns remain as documentation and fallback.
  </action>
  <verify>
    <automated>grep -c "ref_type" agent/skills/skill-ontology-validation/SKILL.md && grep -c "Phase 22 update" agent/skills/skill-ontology-lessons/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ref_type" agent/skills/skill-ontology-validation/SKILL.md returns at least 2
    - grep -c "No manual address parsing" agent/skills/skill-ontology-validation/SKILL.md returns 1
    - grep -c "Phase 22 update" agent/skills/skill-ontology-lessons/SKILL.md returns 1
    - grep -c "legacy fallback" agent/skills/skill-ontology-lessons/SKILL.md returns 1
    - grep "address-parsing rules, type suffix map" agent/skills/skill-ontology-validation/SKILL.md returns NO match (old text removed)
    - grep "BACnetExternalReference" agent/skills/skill-ontology-lessons/SKILL.md returns at least 2 (existing patterns preserved)
    - grep "analog-input" agent/skills/skill-ontology-lessons/SKILL.md returns at least 1 (existing suffix map preserved)
  </acceptance_criteria>
  <done>Validation skill documents pre-computed fields. Lessons skill adds Phase 22 update note at top of BACnet section while preserving all existing content as legacy fallback.</done>
</task>

</tasks>

<verification>
grep -l "ref_type" agent/skills/skill-ontology-generation/SKILL.md agent/skills/skill-ontology-validation/SKILL.md agent/skills/skill-ontology-lessons/SKILL.md
</verification>

<success_criteria>
- All 3 SKILL.md files reference ref_type, code, and address fields
- Generation and validation skills say "No manual address parsing is needed"
- Lessons skill preserves all existing BACnet content and adds Phase 22 update note
- No references to old "address-parsing rules, type suffix map" pattern in generation or validation skills
</success_criteria>

<output>
After completion, create `.planning/phases/22-enhance-bacnet-parsing/22-03-SUMMARY.md`
</output>

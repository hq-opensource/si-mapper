---
phase: 24-remove-screenshots-to-the-front-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/tools/capture_frontend_state_tool.py
  - agent/tests/test_capture_frontend_state.py
  - agent/tests/test_capture_frontend_state_live.py
  - agent/master_architecture/create_master_agent.py
  - agent/pyproject.toml
  - agent/skills/skill-ductwork/SKILL.md
  - agent/skills/skill-hvac-equipments/SKILL.md
  - agent/tests/test_create_master_agent.py
autonomous: true
requirements:
  - P24-01
  - P24-02
  - P24-03

must_haves:
  truths:
    - "capture_frontend_state_tool is completely removed from the codebase"
    - "Playwright dependency is removed from pyproject.toml"
    - "Ductwork skill has no verification step and exits after sync"
    - "HVAC equipment skill has no verification step and exits after sync"
    - "All existing tests pass (no regressions)"
  artifacts:
    - path: "agent/master_architecture/create_master_agent.py"
      provides: "Master agent without capture_frontend_state_tool"
      contains: "load_ttl_to_neo4j_tool"
    - path: "agent/pyproject.toml"
      provides: "Dependencies without playwright"
    - path: "agent/skills/skill-ductwork/SKILL.md"
      provides: "Simplified ductwork skill (5 steps, no verification)"
    - path: "agent/skills/skill-hvac-equipments/SKILL.md"
      provides: "Simplified equipment skill (6 steps, no verification)"
    - path: "agent/tests/test_create_master_agent.py"
      provides: "Regression guard for capture_frontend_state_tool removal"
  key_links:
    - from: "agent/master_architecture/create_master_agent.py"
      to: "agent/tools/"
      via: "import statements"
      pattern: "capture_frontend_state_tool.*not.*in.*src"
---

<objective>
Remove the Playwright screenshot verification mechanism entirely from the codebase: delete the tool and its tests, remove the import/registration from the master agent, remove the playwright dependency, update both ductwork and HVAC equipment skills to remove verification steps, and add a regression guard test.

Purpose: The agent self-correction loop via screenshots is expensive and unreliable. The new philosophy is: agent places components, syncs, exits. Humans inspect and correct manually.
Output: Clean codebase with no Playwright references, simplified skills, green test suite.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-remove-screenshots-to-the-front-end/24-CONTEXT.md
@.planning/phases/24-remove-screenshots-to-the-front-end/24-RESEARCH.md

<interfaces>
<!-- Key files and their current structure that the executor needs to understand -->

From agent/master_architecture/create_master_agent.py:
- Line 18: `from tools.capture_frontend_state_tool import capture_frontend_state_tool` (DELETE)
- Line 84: `capture_frontend_state_tool,` in task_tools list (DELETE)
- Line 85: `load_ttl_to_neo4j_tool,` stays as next tool after internal grid block

From agent/pyproject.toml:
- Line 19: `"playwright>=1.40.0",` (DELETE)

From agent/tests/test_create_master_agent.py:
- Existing pattern for regression guards: `test_no_ontology_generator_agent_import` reads source and asserts string NOT in src
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tool files and remove registrations</name>
  <files>
    agent/tools/capture_frontend_state_tool.py,
    agent/tests/test_capture_frontend_state.py,
    agent/tests/test_capture_frontend_state_live.py,
    agent/master_architecture/create_master_agent.py,
    agent/pyproject.toml
  </files>
  <read_first>
    agent/master_architecture/create_master_agent.py,
    agent/pyproject.toml
  </read_first>
  <action>
1. Delete these 3 files entirely:
   - `agent/tools/capture_frontend_state_tool.py`
   - `agent/tests/test_capture_frontend_state.py`
   - `agent/tests/test_capture_frontend_state_live.py`

2. Edit `agent/master_architecture/create_master_agent.py`:
   - Remove line 18: `from tools.capture_frontend_state_tool import capture_frontend_state_tool`
   - Remove line 84: `capture_frontend_state_tool,` from the task_tools list
   - The resulting task_tools list should go directly from `update_component_metadata_batch,` to `load_ttl_to_neo4j_tool,` with no blank line between them

3. Edit `agent/pyproject.toml`:
   - Remove line 19: `"playwright>=1.40.0",` from the dependencies list
   - Do NOT remove any other dependency
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper/agent && ! test -f tools/capture_frontend_state_tool.py && ! test -f tests/test_capture_frontend_state.py && ! test -f tests/test_capture_frontend_state_live.py && ! grep -q "capture_frontend_state" master_architecture/create_master_agent.py && ! grep -q "playwright" pyproject.toml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `agent/tools/capture_frontend_state_tool.py` does not exist on disk
    - `agent/tests/test_capture_frontend_state.py` does not exist on disk
    - `agent/tests/test_capture_frontend_state_live.py` does not exist on disk
    - `agent/master_architecture/create_master_agent.py` contains 0 occurrences of `capture_frontend_state`
    - `agent/master_architecture/create_master_agent.py` contains 0 occurrences of `capture_frontend_state_tool`
    - `agent/pyproject.toml` contains 0 occurrences of `playwright`
    - `agent/master_architecture/create_master_agent.py` still contains `load_ttl_to_neo4j_tool`
    - `agent/master_architecture/create_master_agent.py` still contains all other imports (neo4j_query_tools, ontology_tools, etc.)
  </acceptance_criteria>
  <done>3 files deleted, import+registration removed from create_master_agent.py, playwright removed from pyproject.toml</done>
</task>

<task type="auto">
  <name>Task 2: Update skill SKILL.md files and add regression test</name>
  <files>
    agent/skills/skill-ductwork/SKILL.md,
    agent/skills/skill-hvac-equipments/SKILL.md,
    agent/tests/test_create_master_agent.py
  </files>
  <read_first>
    agent/skills/skill-ductwork/SKILL.md,
    agent/skills/skill-hvac-equipments/SKILL.md,
    agent/tests/test_create_master_agent.py
  </read_first>
  <action>
1. Edit `agent/skills/skill-ductwork/SKILL.md`:
   a. In Step 1 (line 14), change:
      `- Use `ingest_category_files(category='hvac')`. **Note the artifact names returned** — you will need them in the verification step.`
      to:
      `- Use `ingest_category_files(category='hvac')`.`
   b. Delete Step 5 entirely (lines 69-75) — the "Verify the ductwork" section including all sub-bullets about capture_frontend_state, load_artifacts with snapshot, correction loop, and sync retry.
   c. Renumber Step 6 to Step 5. The heading becomes `## 5. Exit`
   d. Replace the Step 5 (formerly 6) Exit body with:
      ```
      Call `exit_with_success(summary="...")` to signal completion. The summary **must** include:
      - Number of ducts registered (horizontal and vertical counts)
      - Confirmation that `sync_agent_to_graphivac` succeeded

      Example: "5 ducts registered and synced to frontend (4 horizontal, 1 vertical)"
      ```
   e. Remove the failure path paragraph entirely ("Failure path: If verification cannot be resolved after 3 correction cycles...")

2. Edit `agent/skills/skill-hvac-equipments/SKILL.md`:
   a. In Step 1 (line 15), change:
      `1. **Load context**: Use `load_artifacts` to view drawings. **Note the artifact names of the reference drawings** — you will need them in the verification step.`
      to:
      `1. **Load context**: Use `load_artifacts` to view drawings.`
   b. Delete Step 6 entirely (lines 26-31) — the "Verify" section including all sub-bullets about capture_frontend_state, load_artifacts with snapshot, correction loop.
   c. Renumber Step 7 to Step 6. Update the line to be:
      `6. **Exit**: Call `exit_with_success(summary="...")` to signal completion. The summary **must** include:`
   d. Replace the exit summary bullets with:
      ```
      - Number of equipment pieces placed
      - Confirmation that `sync_agent_to_graphivac` succeeded

      Example: "12 equipment pieces registered and synced to frontend (3 fans, 2 coils, 4 dampers, 1 filter, 2 sensors)"
      ```
   e. Remove the failure path paragraph entirely ("Failure path: If verification cannot be resolved after 3 correction cycles...")

3. Edit `agent/tests/test_create_master_agent.py`:
   Add a new test function after `test_imports_adapted_exit_tools`:
   ```python
   def test_no_capture_frontend_state_tool_import():
       """create_master_agent.py must NOT import capture_frontend_state_tool (removed in Phase 24)."""
       src = pathlib.Path("master_architecture/create_master_agent.py").read_text()
       assert "capture_frontend_state_tool" not in src
   ```
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper/agent && uv run pytest tests/test_create_master_agent.py -x -q && ! grep -q "capture_frontend_state" skills/skill-ductwork/SKILL.md && ! grep -q "capture_frontend_state" skills/skill-hvac-equipments/SKILL.md && ! grep -q "verification step" skills/skill-ductwork/SKILL.md && ! grep -q "verification step" skills/skill-hvac-equipments/SKILL.md && ! grep -q "correction cycle" skills/skill-ductwork/SKILL.md && ! grep -q "correction cycle" skills/skill-hvac-equipments/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `agent/skills/skill-ductwork/SKILL.md` contains 0 occurrences of `capture_frontend_state`
    - `agent/skills/skill-ductwork/SKILL.md` contains 0 occurrences of `verification step`
    - `agent/skills/skill-ductwork/SKILL.md` contains 0 occurrences of `correction cycle`
    - `agent/skills/skill-ductwork/SKILL.md` contains `## 5. Exit` (renumbered from 6)
    - `agent/skills/skill-ductwork/SKILL.md` contains `sync_agent_to_graphivac` in the exit summary
    - `agent/skills/skill-hvac-equipments/SKILL.md` contains 0 occurrences of `capture_frontend_state`
    - `agent/skills/skill-hvac-equipments/SKILL.md` contains 0 occurrences of `verification step`
    - `agent/skills/skill-hvac-equipments/SKILL.md` contains 0 occurrences of `correction cycle`
    - `agent/skills/skill-hvac-equipments/SKILL.md` contains `6. **Exit**` (renumbered from 7)
    - `agent/skills/skill-hvac-equipments/SKILL.md` contains `sync_agent_to_graphivac` in the exit summary
    - `agent/tests/test_create_master_agent.py` contains `def test_no_capture_frontend_state_tool_import`
    - `cd agent && uv run pytest tests/test_create_master_agent.py -x -q` exits 0
  </acceptance_criteria>
  <done>Both skills simplified (verification steps removed, exit summaries updated, steps renumbered), regression guard test added and passing</done>
</task>

</tasks>

<verification>
1. No file named `capture_frontend_state` exists under `agent/`:
   `find agent/ -name "*capture_frontend*" -type f` returns empty
2. No reference to `capture_frontend_state` in any Python or Markdown file under `agent/`:
   `grep -r "capture_frontend_state" agent/ --include="*.py" --include="*.md"` returns empty
3. No reference to `playwright` in `agent/pyproject.toml`:
   `grep "playwright" agent/pyproject.toml` returns empty
4. Full test suite passes:
   `cd agent && uv run pytest tests/ -x -q` exits 0
</verification>

<success_criteria>
- Zero occurrences of `capture_frontend_state` across entire agent/ directory
- Zero occurrences of `playwright` in agent/pyproject.toml
- Both skills have correct step numbering (ductwork: 5 steps, equipment: 6 steps)
- Both skills exit summaries require component count + sync confirmation only
- Regression guard test exists and passes
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/24-remove-screenshots-to-the-front-end/24-01-SUMMARY.md`
</output>

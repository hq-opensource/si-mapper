---
phase: 25-translate-report-to-french
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - report_french/sections/01-introduction.tex
  - report_french/sections/02-problem.tex
autonomous: true
requirements: [TBD-02, TBD-04]

must_haves:
  truths:
    - "Section 01-introduction.tex is entirely in French"
    - "Section 02-problem.tex is entirely in French"
    - "All \\label{} and \\ref{} identifiers are unchanged"
    - "All \\cite{} keys are unchanged"
    - "All table content (headers and data cells with prose) is in French"
    - "All figure captions are in French"
    - "Technical acronyms (BACnet, ASHRAE 223P, HVAC, BAS, BMS, etc.) remain in English"
    - "\\todo{} markers in comments of 02-problem.tex are translated"
  artifacts:
    - path: "report_french/sections/01-introduction.tex"
      provides: "French translation of Introduction and Context"
      contains: "\\section{Introduction et contexte}"
    - path: "report_french/sections/02-problem.tex"
      provides: "French translation of Description of the Problem"
      contains: "\\section{Description du problème}"
  key_links:
    - from: "report_french/sections/01-introduction.tex"
      to: "report_french/main.tex"
      via: "\\input{sections/01-introduction}"
      pattern: "\\\\label\\{sec:introduction\\}"
    - from: "report_french/sections/02-problem.tex"
      to: "report_french/main.tex"
      via: "\\input{sections/02-problem}"
      pattern: "\\\\label\\{sec:problem\\}"
---

<objective>
Translate sections 01-introduction.tex (184 lines) and 02-problem.tex (318 lines) from English to French.

Purpose: These two sections establish the context and problem statement -- translating them gives the core framing of the report in French.
Output: Two fully translated .tex files in report_french/sections/.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-translate-report-to-french/25-RESEARCH.md
@report/sections/01-introduction.tex
@report/sections/02-problem.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate 01-introduction.tex to French</name>
  <files>report_french/sections/01-introduction.tex</files>
  <read_first>
    - report/sections/01-introduction.tex
    - report_french/sections/01-introduction.tex
  </read_first>
  <action>
Rewrite report_french/sections/01-introduction.tex with the full content translated to French. This file is 184 lines.

Section heading translations (use these exact strings):
- `\section{Introduction et contexte}` (keep `\label{sec:introduction}`)
- `\subsection{Les ontologies de bâtiments : un langage standardisé pour les bâtiments}` (keep `\label{subsec:ontologies}`)
- `\subsection{La norme ASHRAE 223P}` (keep `\label{subsec:ashrae-223p}`)
- `\subsection{Avantages et obstacles des modèles sémantiques de bâtiments}` (keep `\label{subsec:advantages-barriers}`)

Table translations:
- Table~\ref{tab:ontologies} caption: `\caption{Les quatre principales normes d'ontologies de bâtiments.}`
  Column headers: `\textbf{Ontologie} & \textbf{Gouverné par} & \textbf{Approche} & \textbf{Domaine} & \textbf{Maturité}`
  Cell translations:
  - "Tag-based dictionary" -> "Dictionnaire à base d'étiquettes"
  - "Equipment types, sensor tags" -> "Types d'équipements, étiquettes de capteurs"
  - "Widely deployed, informal" -> "Largement déployé, informel"
  - "RDF/OWL semantic graph" -> "Graphe sémantique RDF/OWL"
  - "Equipment, sensors, spatial hierarchy" -> "Équipements, capteurs, hiérarchie spatiale"
  - "Production ready (v1.4)" -> "Prêt pour la production (v1.4)"
  - "OWL ontology" -> "Ontologie OWL"
  - "Property management + technical systems" -> "Gestion immobilière + systèmes techniques"
  - "Production ready" -> "Prêt pour la production"
  - "Formal OWL ontology" -> "Ontologie OWL formelle"
  - "Topology, connections, telemetry" -> "Topologie, connexions, télémétrie"
  - "Second advisory public review" -> "Deuxième examen public consultatif"

- Table~\ref{tab:223p-concepts} caption: `\caption{Les six concepts fondamentaux d'ASHRAE 223P.}`
  Column headers: `\textbf{Concept} & \textbf{Définition} & \textbf{Exemple}`
  Cell translations:
  - "Well-defined classes with formal rules governing usage" -> "Classes bien définies avec des règles formelles régissant l'utilisation"
  - "How equipment and spaces connect" -> "Comment les équipements et les espaces se connectent"
  - "Hierarchical grouping of entities" -> "Regroupement hiérarchique des entités"
  - "Observable properties with external data references" -> "Propriétés observables avec des références de données externes"
  - "Descriptive attributes with units" -> "Attributs descriptifs avec unités"
  - "Substance conveyed through connections" -> "Substance transportée à travers les connexions"
  - "Rated airflow in m\textsuperscript{3}/s" -> "Débit d'air nominal en m\textsuperscript{3}/s"
  - "Chilled water, Supply air, Electricity" -> "Eau glacée, Air soufflé, Électricité"

Body text translation rules:
- Translate all English prose to French
- Keep all \texttt{...} content unchanged (code strings like 2500.AI11, RTU-3/SA-T, AHU-1.SA-T)
- Keep all \emph{...} but translate the text inside
- Keep all \textbf{...} markers like \textbf{Project Haystack}, \textbf{Brick Schema}, \textbf{RealEstateCore}, \textbf{ASHRAE 223P}, \textbf{SI-Mapper} -- translate surrounding prose, keep proper names
- Keep all \cite{...} keys unchanged
- Keep all \label{...} and \ref{...} identifiers unchanged
- Keep all \begin{...}, \end{...} environments unchanged
- "Building Management System (BMS)" -> "Système de gestion technique du bâtiment (GTB/BMS)"
- "building ontologies" -> "ontologies de bâtiments"
- "semantic building model" -> "modèle sémantique de bâtiment"
- Use "CVC (HVAC)" on first mention of HVAC, then "CVC" or "HVAC" consistently
- Use French guillemets with \og and \fg for quoted text instead of English double quotes
  </action>
  <verify>
    <automated>grep -c "\\\\section{Introduction et contexte}" /home/juan/codes/si-mapper/report_french/sections/01-introduction.tex && grep -c "\\\\label{sec:introduction}" /home/juan/codes/si-mapper/report_french/sections/01-introduction.tex && grep -c "\\\\label{subsec:ontologies}" /home/juan/codes/si-mapper/report_french/sections/01-introduction.tex && grep -c "\\\\label{subsec:ashrae-223p}" /home/juan/codes/si-mapper/report_french/sections/01-introduction.tex && grep -c "\\\\label{subsec:advantages-barriers}" /home/juan/codes/si-mapper/report_french/sections/01-introduction.tex && grep -c "\\\\cite{Wang2017}" /home/juan/codes/si-mapper/report_french/sections/01-introduction.tex | head -1 && echo "SEC01 OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\\section{Introduction et contexte}" returns 1
    - grep "\\label{sec:introduction}" returns 1 (unchanged)
    - grep "\\label{subsec:ontologies}" returns 1 (unchanged)
    - grep "\\label{subsec:ashrae-223p}" returns 1 (unchanged)
    - grep "\\label{subsec:advantages-barriers}" returns 1 (unchanged)
    - grep "\\label{tab:ontologies}" returns 1 (unchanged)
    - grep "\\label{tab:223p-concepts}" returns 1 (unchanged)
    - grep "\\ref{tab:" returns same count as original (cross-references preserved)
    - No English section headings remain (grep "Introduction and Context" returns 0)
    - No English table captions remain (grep "The four major building" returns 0)
    - All \texttt{} code strings unchanged (grep "2500.AI11" returns 1)
    - File has approximately 184 lines (within +/- 20 lines due to translation length differences)
  </acceptance_criteria>
  <done>01-introduction.tex fully translated to French with all LaTeX structure, labels, references, and code strings preserved</done>
</task>

<task type="auto">
  <name>Task 2: Translate 02-problem.tex to French</name>
  <files>report_french/sections/02-problem.tex</files>
  <read_first>
    - report/sections/02-problem.tex
    - report_french/sections/02-problem.tex
  </read_first>
  <action>
Rewrite report_french/sections/02-problem.tex with the full content translated to French. This file is 318 lines.

Section heading translations (use these exact strings):
- `\section{Description du problème}` (keep `\label{sec:problem}`)
- `\subsection{Le problème du mappage des points BACnet}` (keep `\label{subsec:bacnet-problem}`)
- `\subsection{Le coût du mappage manuel}` (keep `\label{subsec:cost}`)
- `\subsection{Le fossé de recherche et les solutions émergentes}` (keep `\label{subsec:research-gap}`)
- `\subsection{Pourquoi cela importe pour le réseau électrique}` (keep `\label{subsec:grid-relevance}`)

Table translations:
- Table~\ref{tab:commissioning-costs} caption: `\caption{Données clés de coûts et d'efforts pour la mise en service manuelle des systèmes GTB et le mappage des points.}`
  Column headers: `\textbf{Poste de coût} & \textbf{Chiffres} & \textbf{Source}`
  Translate the Cost Item column entries:
  - "BAS commissioning as % of construction" -> "Mise en service GTB en \% de la construction"
  - "Case study: 150,000 sq ft school" -> "Étude de cas : école de 150\,000\,pi\textsuperscript{2}"
  - "Complete BAS installation average" -> "Installation GTB complète moyenne"
  - "Legacy BMS integration premium" -> "Surcoût d'intégration de GTB existant"
  - "Additional service calls from poor mapping" -> "Appels de service supplémentaires liés à un mauvais mappage"
  - "Energy performance drift from poor commissioning" -> "Dérive de performance énergétique due à une mauvaise mise en service"
  - "First-year unplanned expenses (case study school)" -> "Dépenses imprévues la première année (étude de cas école)"
  - "HVAC technician shortage (2024-2025)" -> "Pénurie de techniciens CVC (2024--2025)"

- Table~\ref{tab:landscape} caption: `\caption{Paysage concurrentiel pour le mappage automatisé des points GTB et la génération de modèles sémantiques (2024--2026).}`
  Column headers: `\textbf{Solution} & \textbf{Catégorie} & \textbf{Entrée} & \textbf{Écart par rapport à SI-Mapper}`
  Translate the Category, Input, and Gap cells for each row.

Comment lines at top of file (lines 1-28): translate the English comment text to French. For example:
- "% CHANGES FROM ORIGINAL:" -> "% MODIFICATIONS PAR RAPPORT À L'ORIGINAL :"
- Translate each numbered change note

Body text translation rules (same as Task 1):
- Translate all English prose to French
- Keep all \texttt{...} content unchanged
- Keep all \cite{...} keys unchanged
- Keep all \label{...} and \ref{...} identifiers unchanged
- "Building Automation System (BAS)" -> "Système d'automatisation du bâtiment (GTB/BAS)"
- "Virtual Power Plants" -> "Centrales électriques virtuelles" (or "centrales virtuelles" for brevity after first use)
- "Non-Wire Alternatives" -> "Alternatives non filaires"
- Quoted text: use \og and \fg for French guillemets
- Keep all monetary values, percentages, and units unchanged
- The \todo{} in the comments (line 26) should be translated: `\todo{commentaires afin de les retrouver avant la soumission finale}`

CRITICAL: Do NOT modify any \label{}, \ref{}, \cite{} identifiers. Do NOT modify any \texttt{} content. Do NOT modify numerical data in tables.
  </action>
  <verify>
    <automated>grep -c "\\\\section{Description du problème}" /home/juan/codes/si-mapper/report_french/sections/02-problem.tex && grep -c "\\\\label{sec:problem}" /home/juan/codes/si-mapper/report_french/sections/02-problem.tex && grep -c "\\\\label{subsec:bacnet-problem}" /home/juan/codes/si-mapper/report_french/sections/02-problem.tex && grep -c "\\\\label{subsec:cost}" /home/juan/codes/si-mapper/report_french/sections/02-problem.tex && grep -c "\\\\label{subsec:research-gap}" /home/juan/codes/si-mapper/report_french/sections/02-problem.tex && grep -c "\\\\label{subsec:grid-relevance}" /home/juan/codes/si-mapper/report_french/sections/02-problem.tex && grep -c "\\\\label{tab:commissioning-costs}" /home/juan/codes/si-mapper/report_french/sections/02-problem.tex && grep -c "\\\\label{tab:landscape}" /home/juan/codes/si-mapper/report_french/sections/02-problem.tex && echo "SEC02 OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\\section{Description du problème}" returns 1
    - grep "\\label{sec:problem}" returns 1 (unchanged)
    - grep "\\label{subsec:bacnet-problem}" returns 1 (unchanged)
    - grep "\\label{subsec:cost}" returns 1 (unchanged)
    - grep "\\label{subsec:research-gap}" returns 1 (unchanged)
    - grep "\\label{subsec:grid-relevance}" returns 1 (unchanged)
    - grep "\\label{tab:commissioning-costs}" returns 1 (unchanged)
    - grep "\\label{tab:landscape}" returns 1 (unchanged)
    - All \cite{} keys preserved: grep "\\cite{Trenbath2022}" returns at least 1, grep "\\cite{Wang2017}" returns at least 1, grep "\\cite{WoodMac2025}" returns 1
    - No English section headings remain: grep "Description of the Problem" returns 0
    - No English table captions remain: grep "Key cost and effort data" returns 0
    - grep "Centrales électriques virtuelles" returns at least 1 (VPP translated)
    - File has approximately 318 lines (within +/- 30 lines)
  </acceptance_criteria>
  <done>02-problem.tex fully translated to French with all LaTeX structure, labels, references, tables, and code strings preserved</done>
</task>

</tasks>

<verification>
- Both section files contain only French prose (no untranslated English paragraphs)
- All \label{}, \ref{}, \cite{} identifiers match the originals exactly
- All \texttt{} code strings unchanged
- pdflatex compiles report_french/ without errors (sections 03-05 still in English is fine)
</verification>

<success_criteria>
- 01-introduction.tex has 4 French section headings, 2 translated tables, all prose in French
- 02-problem.tex has 5 French section headings, 2 translated tables, all prose in French
- Zero modified \label{}, \ref{}, or \cite{} identifiers
- Technical acronyms (BACnet, ASHRAE 223P, HVAC, etc.) preserved in English
</success_criteria>

<output>
After completion, create `.planning/phases/25-translate-report-to-french/25-02-SUMMARY.md`
</output>

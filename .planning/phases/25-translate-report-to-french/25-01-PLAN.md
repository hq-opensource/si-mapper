---
phase: 25-translate-report-to-french
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - report_french/main.tex
  - report_french/references.bib
  - report_french/figures/
  - report_french/diagrams/
  - report_french/sections/
autonomous: true
requirements: [TBD-01, TBD-05, TBD-03, TBD-06]

must_haves:
  truths:
    - "report_french/ directory exists with identical structure to report/"
    - "report/ directory is completely unmodified"
    - "main.tex has babel[french] in preamble"
    - "Title, abstract, and date are translated to French"
    - "references.bib title fields are translated to French"
    - "All figures and diagrams are copied verbatim"
  artifacts:
    - path: "report_french/main.tex"
      provides: "French preamble with babel, translated title/abstract"
      contains: "\\usepackage[french]{babel}"
    - path: "report_french/references.bib"
      provides: "Bibliography with translated title fields"
    - path: "report_french/figures/"
      provides: "Pre-rendered PNG figures (verbatim copy)"
    - path: "report_french/diagrams/"
      provides: "Mermaid source files (verbatim copy)"
  key_links:
    - from: "report_french/main.tex"
      to: "report_french/sections/*.tex"
      via: "\\input{sections/...}"
      pattern: "\\\\input\\{sections/"
    - from: "report_french/main.tex"
      to: "report_french/references.bib"
      via: "\\bibliography{references}"
      pattern: "\\\\bibliography\\{references\\}"
---

<objective>
Copy the entire report/ folder to report_french/, add French babel localization to the preamble, translate main.tex (title, abstract, date) and references.bib (title fields) to French.

Purpose: Establish the French report folder structure and translate the two non-section files so that subsequent plans can translate section files independently.
Output: report_french/ with translated main.tex and references.bib, all figures/diagrams copied verbatim.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-translate-report-to-french/25-RESEARCH.md
@report/main.tex
@report/references.bib
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy report/ to report_french/ and verify structure</name>
  <files>report_french/</files>
  <read_first>
    - report/main.tex
  </read_first>
  <action>
Run the following commands:
```bash
cp -r /home/juan/codes/si-mapper/report /home/juan/codes/si-mapper/report_french
```

Then delete any compiled artifacts from report_french/ that should not be committed:
```bash
rm -f /home/juan/codes/si-mapper/report_french/main.aux
rm -f /home/juan/codes/si-mapper/report_french/main.log
rm -f /home/juan/codes/si-mapper/report_french/main.out
rm -f /home/juan/codes/si-mapper/report_french/main.toc
rm -f /home/juan/codes/si-mapper/report_french/main.bbl
rm -f /home/juan/codes/si-mapper/report_french/main.blg
rm -f /home/juan/codes/si-mapper/report_french/main.pdf
rm -f /home/juan/codes/si-mapper/report_french/report.md
```

Verify the structure matches:
```bash
ls -R report_french/sections/ report_french/figures/ report_french/diagrams/
```

Verify report/ is unmodified:
```bash
git diff report/
```
  </action>
  <verify>
    <automated>ls /home/juan/codes/si-mapper/report_french/main.tex /home/juan/codes/si-mapper/report_french/references.bib /home/juan/codes/si-mapper/report_french/sections/01-introduction.tex /home/juan/codes/si-mapper/report_french/sections/05-conclusions.tex /home/juan/codes/si-mapper/report_french/figures/pipeline.png /home/juan/codes/si-mapper/report_french/diagrams/pipeline.mmd && echo "STRUCTURE OK"</automated>
  </verify>
  <acceptance_criteria>
    - report_french/main.tex exists
    - report_french/references.bib exists
    - report_french/sections/ contains 01-introduction.tex, 02-problem.tex, 03-solution.tex, 04-experiment.tex, 05-conclusions.tex
    - report_french/figures/ contains all 11 image files (pipeline.png, architecture.png, experiment-progression.png, system_one_original.png, system_one_ia_replicated.jpg, system_one_graph_full.jpg, system_one_graph_detail.jpg, system_three.png, system_three_ia_replicated.jpg, system_three_graph_full.jpg, system_three_graph_detail.jpg)
    - report_french/diagrams/ contains pipeline.mmd, architecture.mmd, experiment-progression.mmd
    - No .aux, .log, .out, .toc, .bbl, .blg, .pdf files in report_french/
    - git diff report/ produces empty output
  </acceptance_criteria>
  <done>report_french/ directory exists with full structure, no compiled artifacts, report/ unmodified</done>
</task>

<task type="auto">
  <name>Task 2: Translate main.tex preamble, title, abstract, and date to French</name>
  <files>report_french/main.tex</files>
  <read_first>
    - report_french/main.tex
    - report/main.tex
  </read_first>
  <action>
Edit report_french/main.tex with these specific changes:

1. Add `\usepackage[french]{babel}` immediately after `\usepackage[utf8]{inputenc}` (line 7). The result should be:
```latex
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage{lmodern}
```

2. Replace the title (line 40-41) with:
```latex
\title{SI-Mapper : Intelligence artificielle agentique pour la modélisation sémantique automatisée des systèmes CVC\\[0.5em]
\large Des données BACnet et des plans d'ingénierie aux ontologies ASHRAE 223P}
```

3. Replace the author line (line 42) with:
```latex
\author{Juan [Nom de famille] \\ \textit{[Affiliation]}}
```

4. Replace the date (line 43) with:
```latex
\date{Mars 2026}
```

5. Replace the abstract (lines 49-51, the text between \begin{abstract} and \end{abstract}) with:
```latex
\begin{abstract}
Ce rapport présente SI-Mapper, un système d'intelligence artificielle agentique qui automatise la création de modèles sémantiques de bâtiments conformes à ASHRAE 223P à partir de plans d'ingénierie CVC (HVAC) et de données de points BACnet. Nous décrivons le problème du mappage manuel des points BACnet --- un processus coûteux et intensif en main-d'œuvre qui bloque l'interopérabilité des logiciels de bâtiment et l'intégration au réseau électrique --- et présentons une solution basée sur l'IA qui remplace des semaines de travail d'ingénierie experte. Le rapport retrace le développement à travers neuf itérations expérimentales, depuis les approches simples d'appels de fonctions jusqu'à l'architecture finale à agent maître unique basée sur les compétences. Deux expériences sur des installations réelles d'unités de traitement d'air démontrent l'efficacité du système : le Système 1 (21 composants CVC, 44 points BACnet) a produit un modèle ASHRAE 223P validé en 17 minutes pour un coût estimé de 11,30\,\$ USD ; le Système 3 (27 composants, 34 points BACnet) a été complété en 12 minutes pour 10,85\,\$ USD. Dans les deux cas, l'agent a corrigé ses propres erreurs d'ontologie sans intervention humaine. Une comparaison avec un ingénieur humain comme référence est en cours.
\end{abstract}
```

CRITICAL: Do NOT modify any \input{}, \bibliographystyle{}, \bibliography{}, \label{}, or \graphicspath{} commands. Do NOT modify the \newcommand{\todo} definition. Do NOT modify any package imports other than adding babel.
  </action>
  <verify>
    <automated>grep -c "\\\\usepackage\[french\]{babel}" /home/juan/codes/si-mapper/report_french/main.tex && grep -c "Mars 2026" /home/juan/codes/si-mapper/report_french/main.tex && grep -c "Intelligence artificielle agentique" /home/juan/codes/si-mapper/report_french/main.tex && grep -c "Ce rapport présente SI-Mapper" /home/juan/codes/si-mapper/report_french/main.tex && grep -c "\\\\input{sections/01-introduction}" /home/juan/codes/si-mapper/report_french/main.tex && grep -c "\\\\bibliography{references}" /home/juan/codes/si-mapper/report_french/main.tex && echo "MAIN.TEX OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "usepackage\[french\]{babel}" report_french/main.tex returns 1 match
    - grep "Mars 2026" report_french/main.tex returns 1 match
    - grep "Intelligence artificielle agentique" report_french/main.tex returns 1 match
    - grep "Ce rapport présente SI-Mapper" report_french/main.tex returns 1 match
    - grep "\\input{sections/01-introduction}" report_french/main.tex returns 1 match (unchanged)
    - grep "\\bibliography{references}" report_french/main.tex returns 1 match (unchanged)
    - grep "\\newcommand{\\todo}" report_french/main.tex returns 1 match (unchanged)
    - grep "\\graphicspath{{figures/}}" report_french/main.tex returns 1 match (unchanged)
    - No \label{} strings modified from original
  </acceptance_criteria>
  <done>main.tex has babel[french], French title, French abstract, French date, all LaTeX structure intact</done>
</task>

<task type="auto">
  <name>Task 3: Translate references.bib title fields to French</name>
  <files>report_french/references.bib</files>
  <read_first>
    - report_french/references.bib
    - report/references.bib
  </read_first>
  <action>
Edit report_french/references.bib. Translate ONLY the `title` field values and `note` field values to French. Do NOT modify: entry keys (@techreport{Trenbath2022), author fields, institution fields, url fields, year/month/volume/pages/doi/number fields, howpublished fields that are proper names.

Specific title translations (apply these exactly):

1. Trenbath2022: `title = {Systèmes de capteurs et de contrôle des bâtiments commerciaux : {Obstacles, facteurs et coûts}}`
   note = `{Préparé pour le Bureau des technologies du bâtiment du Département de l'énergie des États-Unis}`

2. PingCx2024: `title = {Les coûts cachés de la mise en service manuelle dans les bâtiments complexes d'aujourd'hui}`

3. Envigilance2023: `title = {Coût de la surveillance énergétique des bâtiments}`

4. AccessCoins2025: `title = {Surmonter la pénurie de techniciens {HVAC}}`

5. BCA2024: `title = {Mise en service des bâtiments : {Coûts et avantages} (Guide de l'industrie)}`

6. Wang2017: `title = {Mappage automatisé des points pour les systèmes de contrôle des bâtiments : {Avancées} récentes et besoins futurs de recherche}`
   note = `{Disponible via OSTI ID 1495345}`

7. Perini2025: `title = {{BrickLLM} : une bibliothèque {Python} pour la génération de graphes {RDF} conformes à {Brick} à l'aide de {LLMs}}`

8. WoodMac2025: `title = {Rapport 2025 sur le marché des centrales électriques virtuelles en {Amérique du Nord}}`

9. DOE_VPP2025: `title = {Voies vers le décollage commercial : {Centrales électriques virtuelles} Mise à jour 2025}`

10. CEC_EPRI2024: `title = {Approches de centrales électriques virtuelles pour la flexibilité de la demande ({VPP-FLEX}), Subvention {GFO-23-309}}`

11. RMI_VP3_2025: `title = {Rapport d'avancement {VP3} : De la sensibilisation à l'action}`

12. SEPA_NCCETC2025: `title = {50 États des centrales électriques virtuelles et des ressources énergétiques distribuées de soutien : Aperçu des politiques étatiques 2024}`

13. BuildingMOTIF_NREL: `title = {{BuildingMOTIF} : Cadre d'interopérabilité des ontologies de métadonnées du bâtiment}`

14. DOE_BuildingMOTIF: `title = {Modélisation sémantique et interopérabilité}`

15. Open223: `title = {Modèles de référence {ASHRAE} 223P {Open223}}`

16. Willow2025: `title = {Plateforme de jumeau numérique pilotée par l'{IA} Willow}`

17. SkySpark: `title = {Plateforme d'analyse {SkySpark}}`

18. mathew2015eis: `title = {Mégadonnées pour la performance énergétique des bâtiments : Leçons tirées de l'assemblage d'une très grande base de données nationale sur la consommation énergétique des bâtiments}`
    note = `{Rapporte des coûts initiaux par point de SIE de 100\,\$--500\,\$ pour 26 organisations}`

19. Sivasubramaniam2024: `title = {{SM3-Text-to-Query} : Référentiel synthétique multi-modèle de texte vers requête médicale}`
    note = `{Compare les performances des LLM pour la conversion texte-requête en Cypher, SPARQL et SQL ; constate que les LLM obtiennent une meilleure précision en Cypher qu'en SPARQL}`

Also translate all French comment lines (lines starting with %) to French. For example:
- `% --- Études de coûts BAS / BACnet de base ---`
- `% --- Revue systématique du mappage automatisé des points ---`
- `% --- BrickLLM (travail académique le plus proche) ---`
- etc.

CRITICAL: Do NOT modify any @type{key, lines. Do NOT modify author, institution, url, doi, year, month, volume, pages, number, or howpublished fields.
  </action>
  <verify>
    <automated>grep -c "@techreport{Trenbath2022" /home/juan/codes/si-mapper/report_french/references.bib && grep -c "@article{Wang2017" /home/juan/codes/si-mapper/report_french/references.bib && grep -c "Obstacles, facteurs et coûts" /home/juan/codes/si-mapper/report_french/references.bib && grep -c "Centrales électriques virtuelles" /home/juan/codes/si-mapper/report_french/references.bib && echo "BIB OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 19 entry keys unchanged: grep "@techreport{Trenbath2022" returns 1, grep "@article{Wang2017" returns 1, grep "@misc{PingCx2024" returns 1, etc.
    - grep "Obstacles, facteurs et coûts" report_french/references.bib returns 1 match
    - grep "Les coûts cachés" report_french/references.bib returns 1 match
    - grep "Centrales électriques virtuelles" report_french/references.bib returns at least 3 matches
    - grep "Mappage automatisé des points" report_french/references.bib returns 1 match
    - No author fields modified (grep "Trenbath, Kim" still returns 1)
    - No url fields modified (grep "docs.nrel.gov" still returns 1)
  </acceptance_criteria>
  <done>references.bib has all title fields translated to French, all entry keys and metadata fields preserved</done>
</task>

</tasks>

<verification>
- report_french/ directory exists with full structure
- main.tex compiles with pdflatex (may have undefined refs for sections not yet translated -- that is expected)
- report/ is unmodified (git diff report/ is empty)
- All \label{}, \cite{}, \ref{} identifiers unchanged
</verification>

<success_criteria>
- report_french/ folder exists with main.tex, references.bib, sections/, figures/, diagrams/
- babel[french] is in the preamble
- Title, abstract, date are in French
- All 19 bib entry keys unchanged, title fields translated
- Figures and diagrams copied verbatim
- report/ unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/25-translate-report-to-french/25-01-SUMMARY.md`
</output>

---
phase: 25-translate-report-to-french
plan: 03
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - report_french/sections/03-solution.tex
  - report_french/sections/04-experiment.tex
  - report_french/sections/05-conclusions.tex
autonomous: true
requirements: [TBD-02, TBD-04]

must_haves:
  truths:
    - "Section 03-solution.tex is entirely in French"
    - "Section 04-experiment.tex is entirely in French"
    - "Section 05-conclusions.tex is entirely in French"
    - "All 16 \\todo{} markers in 04-experiment.tex are translated to French (not removed)"
    - "All \\label{} and \\ref{} identifiers are unchanged"
    - "All \\cite{} keys are unchanged"
    - "All \\includegraphics paths are unchanged"
    - "All figure captions are in French"
  artifacts:
    - path: "report_french/sections/03-solution.tex"
      provides: "French translation of SI-Mapper solution description"
      contains: "\\section{SI-Mapper : une approche agentique"
    - path: "report_french/sections/04-experiment.tex"
      provides: "French translation of experiments and results"
      contains: "\\section{Description des expériences"
    - path: "report_french/sections/05-conclusions.tex"
      provides: "French translation of conclusions"
      contains: "\\section{Conclusions}"
  key_links:
    - from: "report_french/sections/04-experiment.tex"
      to: "report_french/main.tex"
      via: "\\todo{} markers preserved"
      pattern: "\\\\todo\\{"
---

<objective>
Translate sections 03-solution.tex (363 lines), 04-experiment.tex (266 lines), and 05-conclusions.tex (143 lines) from English to French.

Purpose: Complete the translation of all section files. Section 04 contains 16 \todo{} markers that must be translated (not removed).
Output: Three fully translated .tex files in report_french/sections/.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-translate-report-to-french/25-RESEARCH.md
@report/sections/03-solution.tex
@report/sections/04-experiment.tex
@report/sections/05-conclusions.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate 03-solution.tex to French</name>
  <files>report_french/sections/03-solution.tex</files>
  <read_first>
    - report/sections/03-solution.tex
    - report_french/sections/03-solution.tex
  </read_first>
  <action>
Rewrite report_french/sections/03-solution.tex with the full content translated to French. This file is 363 lines.

Section heading translations (use these exact strings):
- `\section{SI-Mapper : une approche agentique de la modélisation sémantique des bâtiments}` (keep `\label{sec:implementation}`)
- `\subsection{Vue d'ensemble de l'approche}` (keep `\label{subsec:si-mapper-overview}`)
- `\subsection{Méthodologie de développement : neuf itérations expérimentales}` (keep `\label{subsec:iterations}`)
- `\subsection{Architecture finale}` (keep `\label{subsec:final-architecture}`)
- `\subsection{Compétences}` (keep `\label{subsec:skills}`)
- `\subsection{Outils}` (keep `\label{subsec:tools}`)
- `\subsection{Décisions technologiques clés}` (keep `\label{subsec:technology-decisions}`)

Figure caption translations:
- Fig pipeline: `\caption{Pipeline de données SI-Mapper --- des données brutes du bâtiment au modèle sémantique et à la visualisation en graphe.}`
- Fig architecture: `\caption{Architecture finale de SI-Mapper --- un agent maître unique avec des compétences spécialisées, des outils et une interface de visualisation.}`
- Fig experiment-progression: `\caption{Progression des neuf itérations expérimentales --- des appels de fonctions aux compétences.}`

Table translations -- translate ALL table column headers and cell text:
- Iterations table: Column headers "Iteration" -> "Itération", "Architecture" stays same or translate, "Outcome \& Lesson" -> "Résultat et leçon"
- Skills table: "Skill" -> "Compétence", "Responsibility" -> "Responsabilité"
  - Translate each skill's responsibility description
- Tools table: "Category" -> "Catégorie", "Tools" -> "Outils", "Purpose" -> "Fonction"
  - Translate each tool category and purpose
- Technology decisions table: "Decision" -> "Décision", "Choice" -> "Choix", "Rationale" -> "Justification"
  - Translate each decision description and rationale

CRITICAL rules:
- Keep ALL \label{} identifiers unchanged (sec:implementation, subsec:si-mapper-overview, etc.)
- Keep ALL \ref{} and \cite{} identifiers unchanged
- Keep ALL \includegraphics paths unchanged (pipeline.png, architecture.png, experiment-progression.png)
- Keep ALL \texttt{} content unchanged (code/file names)
- Keep tool names in English when they are code identifiers (e.g., read_internal_grid, write_ontology)
- Keep model names in English (Gemini, Claude Sonnet)
- Keep framework names in English (ADK, Google Agent Development Kit)
- Translate surrounding prose to French
- Use "agent maître" for "master agent"
- Use "compétence(s)" for "skill(s)"
- Use "outil(s)" for "tool(s)"
  </action>
  <verify>
    <automated>grep -c "\\\\label{sec:implementation}" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && grep -c "\\\\label{subsec:si-mapper-overview}" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && grep -c "\\\\label{subsec:iterations}" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && grep -c "\\\\label{subsec:final-architecture}" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && grep -c "\\\\label{subsec:skills}" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && grep -c "\\\\label{subsec:tools}" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && grep -c "\\\\label{subsec:technology-decisions}" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && grep -c "pipeline.png" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && grep -c "architecture.png" /home/juan/codes/si-mapper/report_french/sections/03-solution.tex && echo "SEC03 OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 \label{} identifiers present and unchanged
    - grep "pipeline.png" returns 1 (image path unchanged)
    - grep "architecture.png" returns 1 (image path unchanged)
    - grep "experiment-progression.png" returns 1 (image path unchanged)
    - grep "une approche agentique" returns 1 (French section heading)
    - grep "An Agentic Approach" returns 0 (no English heading remnant)
    - File has approximately 363 lines (within +/- 40 lines)
  </acceptance_criteria>
  <done>03-solution.tex fully translated to French with all LaTeX structure, labels, references, image paths, and code identifiers preserved</done>
</task>

<task type="auto">
  <name>Task 2: Translate 04-experiment.tex to French (16 \todo{} markers must be preserved)</name>
  <files>report_french/sections/04-experiment.tex</files>
  <read_first>
    - report/sections/04-experiment.tex
    - report_french/sections/04-experiment.tex
  </read_first>
  <action>
Rewrite report_french/sections/04-experiment.tex with the full content translated to French. This file is 266 lines and contains 16 \todo{} markers that MUST be translated (not removed).

Section heading translations (use these exact strings):
- `\section{Description des expériences, analyse et résultats}` (keep `\label{sec:results}`)
- `\subsection{Configuration expérimentale}` (no label in original)
- `\subsection{Système 1 : unité de traitement d'air}` (no label in original)
- `\subsubsection{Matériaux d'entrée}` (no label in original)
- `\subsubsection{Métriques de performance}` (no label in original)
- `\subsubsection{Évaluation de la qualité du graphe}` (no label in original)
- `\subsection{Système 3 : unité de traitement d'air}` (no label in original)
- `\subsubsection{Matériaux d'entrée}` (no label in original)
- `\subsubsection{Métriques de performance}` (no label in original)
- `\subsubsection{Évaluation de la qualité du graphe}` (no label in original)
- `\subsection{Référence humaine : ingénieur}` (no label in original)
- `\subsection{Comparaison : agent IA vs ingénieur humain}` (no label in original)

\todo{} marker translations (16 markers -- translate text INSIDE \todo{}, keep the \todo command):
- `\todo{Fill after human trial}` (appears 6 times) -> `\todo{À compléter après l'essai humain}`
- `\todo{Human time}` (appears 2 times) -> `\todo{Temps humain}`
- `\todo{Nx faster}` (appears 2 times) -> `\todo{Nx plus rapide}`
- `\todo{Human cost}` (appears 2 times) -> `\todo{Coût humain}`
- `\todo{Nx cheaper}` (appears 2 times) -> `\todo{Nx moins cher}`
- `\todo{Write comparative analysis once both System 3 AI experiment and human baseline trials...}` (appears 1 time) -> `\todo{Rédiger l'analyse comparative une fois les expériences IA du Système 3 et les essais de référence humaine terminés}`

Figure caption translations:
- System 1 original/replicated figure: translate caption to French, keep \label{fig:system1-comparison} and image paths
- System 1 graph figures: translate captions, keep \label{fig:system1-graph} and image paths
- System 3 figures: same pattern, keep all labels and image paths

Table translations:
- Performance metrics tables: translate column headers and row labels
  - "Metric" -> "Métrique", "Value" -> "Valeur" (or similar)
  - "Total tokens" -> "Tokens totaux", "Estimated cost" -> "Coût estimé", "Wall-clock time" -> "Temps réel"
- Comparison tables: translate all column headers and prose cells, preserve \todo{} markers
- Human baseline table: translate all headers and cells

CRITICAL rules:
- PRESERVE all 16 \todo{} markers (translate text inside, keep \todo command)
- Keep ALL \label{} identifiers unchanged
- Keep ALL \ref{} identifiers unchanged
- Keep ALL \includegraphics paths unchanged
- Keep ALL numerical values unchanged ($11.30, 17 minutes, etc.)
- Keep BACnet point counts, component counts unchanged
- Keep model name "Gemini 3.1 Pro Preview" unchanged
  </action>
  <verify>
    <automated>grep -c "\\\\todo{" /home/juan/codes/si-mapper/report_french/sections/04-experiment.tex | xargs -I{} test {} -ge 16 && echo "TODO_COUNT_OK" || echo "TODO_COUNT_FAIL"; grep -c "\\\\label{sec:results}" /home/juan/codes/si-mapper/report_french/sections/04-experiment.tex && grep -c "À compléter après" /home/juan/codes/si-mapper/report_french/sections/04-experiment.tex && grep -c "Description des expériences" /home/juan/codes/si-mapper/report_french/sections/04-experiment.tex && echo "SEC04 OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "\\todo{" returns at least 16 (all todo markers preserved)
    - grep "À compléter après l'essai humain" returns 6 matches
    - grep "Temps humain" returns 2 matches
    - grep "Nx plus rapide" returns 2 matches
    - grep "Coût humain" returns 2 matches
    - grep "Nx moins cher" returns 2 matches
    - grep "\\label{sec:results}" returns 1 (unchanged)
    - grep "Description des expériences" returns 1 (French heading)
    - grep "Experiment description" returns 0 (no English heading remnant)
    - grep "system_one_original.png" returns 1 (image path unchanged)
    - grep "system_three.png" returns 1 (image path unchanged)
    - File has approximately 266 lines (within +/- 30 lines)
  </acceptance_criteria>
  <done>04-experiment.tex fully translated with all 16 \todo{} markers translated and preserved, all labels and image paths intact</done>
</task>

<task type="auto">
  <name>Task 3: Translate 05-conclusions.tex to French</name>
  <files>report_french/sections/05-conclusions.tex</files>
  <read_first>
    - report/sections/05-conclusions.tex
    - report_french/sections/05-conclusions.tex
  </read_first>
  <action>
Rewrite report_french/sections/05-conclusions.tex with the full content translated to French. This file is 143 lines.

Section heading translations (use these exact strings):
- `\section{Conclusions}` (keep `\label{sec:conclusions}`)
- `\subsection{Résumé des contributions}` (keep `\label{subsec:contributions}`)
- `\subsection{Limitations}` (keep `\label{subsec:limitations}`) -- "Limitations" is the same in French
- `\subsection{Travaux futurs}` (keep `\label{subsec:future-work}`)

Body text translation rules:
- Translate all English prose to French
- Keep all monetary values unchanged ($11.30, $10.85, $5,000--$15,000, etc.)
- Keep all technical metrics unchanged (17 minutes 11 seconds, 12 minutes 24 seconds, 295 nodes, 949 relationships, etc.)
- Keep model names unchanged (Gemini, Claude)
- Keep framework names unchanged (ADK, ASHRAE 223P, BACnet)
- Keep proper names unchanged (SI-Mapper, Graphivac, Neo4j)
- "Virtual Power Plant" -> "Centrale électrique virtuelle"
- "Non-Wire Alternative" -> "Alternative non filaire"
- "agentic AI" -> "IA agentique"
- "semantic building model" -> "modèle sémantique de bâtiment"
- "skills-based architecture" -> "architecture basée sur les compétences"
- "self-correction" -> "autocorrection"
- Use French guillemets with \og and \fg for quoted text

CRITICAL: Keep ALL \label{} identifiers unchanged. Keep ALL \cite{} keys unchanged.
  </action>
  <verify>
    <automated>grep -c "\\\\label{sec:conclusions}" /home/juan/codes/si-mapper/report_french/sections/05-conclusions.tex && grep -c "\\\\label{subsec:contributions}" /home/juan/codes/si-mapper/report_french/sections/05-conclusions.tex && grep -c "\\\\label{subsec:limitations}" /home/juan/codes/si-mapper/report_french/sections/05-conclusions.tex && grep -c "\\\\label{subsec:future-work}" /home/juan/codes/si-mapper/report_french/sections/05-conclusions.tex && grep -c "Résumé des contributions" /home/juan/codes/si-mapper/report_french/sections/05-conclusions.tex && grep -c "Travaux futurs" /home/juan/codes/si-mapper/report_french/sections/05-conclusions.tex && echo "SEC05 OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\\label{sec:conclusions}" returns 1 (unchanged)
    - grep "\\label{subsec:contributions}" returns 1 (unchanged)
    - grep "\\label{subsec:limitations}" returns 1 (unchanged)
    - grep "\\label{subsec:future-work}" returns 1 (unchanged)
    - grep "Résumé des contributions" returns 1 (French heading)
    - grep "Travaux futurs" returns 1 (French heading)
    - grep "Summary of Contributions" returns 0 (no English heading remnant)
    - grep "Future Work" returns 0 (no English heading remnant)
    - File has approximately 143 lines (within +/- 20 lines)
  </acceptance_criteria>
  <done>05-conclusions.tex fully translated to French with all LaTeX structure, labels, and references preserved</done>
</task>

</tasks>

<verification>
- All 3 section files contain only French prose
- All \label{}, \ref{}, \cite{} identifiers match originals
- All \includegraphics paths unchanged
- All 16 \todo{} markers present and translated
- All \texttt{} code strings unchanged
</verification>

<success_criteria>
- 03-solution.tex has 7 French section headings, translated tables, all prose in French
- 04-experiment.tex has 12 French section/subsection headings, 16 translated \todo{} markers, all prose in French
- 05-conclusions.tex has 4 French section headings, all prose in French
- Zero modified \label{}, \ref{}, \cite{}, or \includegraphics identifiers
</success_criteria>

<output>
After completion, create `.planning/phases/25-translate-report-to-french/25-03-SUMMARY.md`
</output>

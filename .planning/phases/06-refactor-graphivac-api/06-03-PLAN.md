---
phase: 06-refactor-graphivac-api
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - tests/test_internal_grid_tools.py
  - tests/test_sync_engine.py
autonomous: false
requirements: [REFAC-01, REFAC-02, REFAC-03, REFAC-04]

must_haves:
  truths:
    - "Internal grid tools correctly add/remove components from ToolContext.state"
    - "Sync engine diff logic correctly identifies creates and deletes"
    - "MCP client maps every component type to the right tool name and args shape"
    - "End-to-end: agent adds components via internal tools, sync service pushes to MCP"
  artifacts:
    - path: "tests/test_internal_grid_tools.py"
      provides: "Unit tests for internal grid tool functions"
      contains: "test_add_component"
    - path: "tests/test_sync_engine.py"
      provides: "Unit tests for sync engine diff and MCP client mapping"
      contains: "test_diff"
  key_links:
    - from: "tests/test_internal_grid_tools.py"
      to: "agent/tools/internal_grid_tools.py"
      via: "direct function import"
      pattern: "from agent.tools.internal_grid_tools import"
    - from: "tests/test_sync_engine.py"
      to: "sync_service/sync_engine.py"
      via: "direct class import"
      pattern: "from sync_service.sync_engine import"
---

<objective>
Create unit tests for the internal grid tools and sync engine, then verify the full system works end-to-end.

Purpose: Validate that the internal state tools and sync service work correctly before using them in production agent runs.

Output: Test files in `tests/`, passing verification, and a human-verified end-to-end check.
</objective>

<execution_context>
@/home/juan/codes/si-mapper/.claude/get-shit-done/workflows/execute-plan.md
@/home/juan/codes/si-mapper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-refactor-graphivac-api/06-CONTEXT.md
@.planning/phases/06-refactor-graphivac-api/06-01-SUMMARY.md
@.planning/phases/06-refactor-graphivac-api/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for internal grid tools and sync engine</name>
  <files>tests/test_internal_grid_tools.py, tests/test_sync_engine.py</files>
  <read_first>
    - agent/tools/internal_grid_tools.py (functions under test)
    - sync_service/sync_engine.py (class under test)
    - sync_service/mcp_client.py (class under test — tool name mapping)
    - tests/test_create_50_fans.py (existing test style reference)
  </read_first>
  <behavior>
    tests/test_internal_grid_tools.py:
    - test_initialize_internal_grid: calling initialize creates empty internal_grid in state
    - test_initialize_idempotent: calling initialize twice doesn't reset state
    - test_add_duct: add_component with type="duct" stores start/end coords
    - test_add_fan: add_component with type="fan" stores coord and rotation
    - test_add_equipment_no_rotation: add_component with type="cooling_coil" stores coord without rotation
    - test_add_sensor: add_component with type="duct_sensor_temperature" stores coord
    - test_add_duplicate_name: adding same name twice returns error string
    - test_add_invalid_type: add_component with type="bogus" returns error string
    - test_add_duct_missing_coords: add_component type="duct" without start/end returns error
    - test_add_fan_missing_coord: add_component type="fan" without coord returns error
    - test_add_components_batch: batch add of 3 components stores all 3
    - test_delete_component: delete by name removes it
    - test_delete_nonexistent: delete unknown name returns error
    - test_delete_batch: batch delete removes multiple
    - test_read_internal_grid_all: read returns all components as JSON
    - test_read_internal_grid_filtered: read with type filter returns subset
    - test_auto_initialize: add_component auto-initializes if internal_grid missing

    tests/test_sync_engine.py:
    - test_diff_empty_to_new: diff from empty to 2 components = 2 creates, 0 deletes
    - test_diff_no_change: same state = 0 creates, 0 deletes
    - test_diff_additions: 1 existing + 1 new = 1 create, 0 deletes
    - test_diff_deletions: 1 existing, current empty = 0 creates, 1 delete
    - test_diff_replacement: remove old + add new in same cycle
    - test_persist_and_load: persist state, create new engine, verify state loaded
    - test_mcp_client_tool_mapping_duct: create_component for duct maps to create_duct with start_coord/end_coord
    - test_mcp_client_tool_mapping_fan: create_component for fan maps to create_fan with coord/rotation
    - test_mcp_client_tool_mapping_sensor: create_component for sensor maps to create_{sensor_type} with coord
  </behavior>
  <action>
Create `tests/test_internal_grid_tools.py`:
- Use a mock ToolContext (create a simple class with a `state` dict attribute — no need for real ADK ToolContext since tools only access `tool_context.state`)
- Test each function by calling it with the mock context and asserting state changes
- Each test function is standalone (creates fresh mock context)

```python
class MockToolContext:
    def __init__(self):
        self.state = {}
```

Create `tests/test_sync_engine.py`:
- Test diff logic directly (pure computation, no MCP calls)
- For MCP client mapping tests, don't call MCP — instead inspect that `create_component` would produce the right tool_name and args by mocking `call_tool` and capturing its arguments
- For persist/load, use `/tmp/test_sync_state_*.json` paths

Both test files should be runnable with: `cd /home/juan/codes/si-mapper && python -m pytest tests/test_internal_grid_tools.py tests/test_sync_engine.py -v`

If pytest is not available, use plain `python -m unittest` as fallback. Structure tests as classes inheriting from `unittest.TestCase` for maximum compatibility.
  </action>
  <verify>
    <automated>cd /home/juan/codes/si-mapper && python -m pytest tests/test_internal_grid_tools.py tests/test_sync_engine.py -v 2>/dev/null || python -m unittest tests.test_internal_grid_tools tests.test_sync_engine -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_internal_grid_tools.py exists and contains at least 15 test methods
    - tests/test_internal_grid_tools.py contains `class MockToolContext`
    - tests/test_internal_grid_tools.py contains `test_add_duct`
    - tests/test_internal_grid_tools.py contains `test_add_fan`
    - tests/test_internal_grid_tools.py contains `test_add_duplicate_name`
    - tests/test_internal_grid_tools.py contains `test_delete_component`
    - tests/test_internal_grid_tools.py contains `test_read_internal_grid`
    - tests/test_sync_engine.py exists and contains at least 7 test methods
    - tests/test_sync_engine.py contains `test_diff_empty_to_new`
    - tests/test_sync_engine.py contains `test_persist_and_load`
    - All tests pass: exit code 0
  </acceptance_criteria>
  <done>All unit tests pass, covering internal grid CRUD operations, duplicate/error handling, sync engine diffing, state persistence, and MCP tool name mapping</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification</name>
  <files>none</files>
  <action>
    Human verifies the complete end-to-end flow. No code changes — this is a verification checkpoint.
    The executor should present the verification steps below to the user and pause for approval.
  </action>
  <verify>Human confirms end-to-end flow works</verify>
  <done>User has confirmed that adding/deleting components via agent internal tools propagates to Graphivac via the sync service within ~2 seconds</done>
  <what-built>
    Complete internal-state architecture:
    1. Agent tools that write to ToolContext.state["internal_grid"] (Plan 01)
    2. Sync service that polls agent state and pushes changes to Graphivac via MCP (Plan 02)
    3. Unit tests validating tool logic and sync engine diff (this plan)
  </what-built>
  <how-to-verify>
    1. Start MCP server: `cd /home/juan/codes/si-mapper && source agent/.venv/bin/activate && python mcp_server/server/main.py`
    2. Start agent: `cd /home/juan/codes/si-mapper && source agent/.venv/bin/activate && python -m agent.main`
    3. Start sync service: `cd /home/juan/codes/si-mapper && source agent/.venv/bin/activate && python -m sync_service.main`
    4. Via the agent chat, ask it to "initialize the internal grid and add a test fan named TEST-FAN at coordinates [5, 5]"
    5. Watch sync service logs — within 2 seconds it should log creating the fan via MCP
    6. Check Graphivac UI — the fan should appear on the grid
    7. Ask the agent to "delete TEST-FAN from the internal grid"
    8. Watch sync service logs — it should log deleting the fan via MCP
    9. Confirm the fan disappears from Graphivac
  </how-to-verify>
  <resume-signal>Type "approved" if the end-to-end flow works, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- All unit tests in tests/test_internal_grid_tools.py pass
- All unit tests in tests/test_sync_engine.py pass
- Human confirms end-to-end flow: agent -> internal state -> sync service -> MCP -> Graphivac
</verification>

<success_criteria>
- Unit tests cover all internal grid tool functions (add, batch add, delete, batch delete, read, initialize)
- Unit tests cover sync engine diff logic (creates, deletes, no-change, replacement)
- Unit tests cover MCP client tool name mapping for duct/fan/sensor types
- Human verified end-to-end: component added via agent tool appears in Graphivac within ~2s
</success_criteria>

<output>
After completion, create `.planning/phases/06-refactor-graphivac-api/06-03-SUMMARY.md`
</output>

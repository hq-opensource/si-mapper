[project]
name = "sync-service"
version = "0.1.0"
description = "Sync service that replicates internal_grid state to GraphyVAC via MCP"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "google-adk[extensions]>=1.18.0",
    "mcp>=1.25.0",
]
